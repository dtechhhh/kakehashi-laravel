---
title: "DECISIONS_LOG"
status: "Batch A/B + DOC-SYNC current"
source_notion_title: "DECISIONS_LOG"
exported_at: "2026-07-28"
authority_rank: "highest"
canonical_source: "Notion"
codex_edit_policy: "read-only"
---

> [!IMPORTANT]
> Controlled read-only snapshot from Notion. Do not change product or domain decisions in a coding task. If this file appears stale or contradictory, stop and ask the operator to verify Notion.

# DECISIONS_LOG — Kakehashi
Catatan keputusan lintas-file yang muncul selama brainstorm MD (vibe engineering).<br>Tujuan: menjaga konsistensi antar-agent (1 agent = 1 file). SETIAP agent brainstormer:<br>- WAJIB membaca log ini di langkah \[0\]/\[1\] sebelum merekomendasikan.<br>- WAJIB menambah entri baru di langkah \[7\] HANDOFF setelah ada keputusan user.
KEBIJAKAN LOG (berlaku semua kelompok):<br>- Hanya ada SATU DECISIONS_LOG.md untuk SELURUH proyek (K1-K4). Jangan membuat log per-kelompok.<br>- Alasan: banyak keputusan lintas-kelompok (mis. TOTP -> K1 ROLES + K2 AUTH + K4 SECURITY).<br>Single source of truth menghindari drift.<br>- Jika nanti terlalu panjang: split horizontal berbasis STATUS (aktif vs arsip), BUKAN per-kelompok.
Aturan:<br>- Hanya catat keputusan yang SUDAH diputuskan user (bukan opsi/usulan).<br>- Jika keputusan mengubah “kebenaran” inti, naikkan versi PRD (jangan tambal diam-diam).<br>- Format tanggal: YYYY-MM-DD (zona Asia/Jakarta).<br>- Setiap entri WAJIB punya baris `Affects:` dan `SUPERSEDES:` (lihat format di bawah).
---
## Status file (papan ringkas)
<table header-row="true">
<tr>
<td>File</td>
<td>Kelompok</td>
<td>Status</td>
<td>Agent/persona</td>
<td>Tgl final</td>
</tr>
<tr>
<td>PROJECT_[OVERVIEW.md](../foundation/PROJECT_OVERVIEW.md)</td>
<td>1 Fondasi</td>
<td>FINAL</td>
<td>Product Strategist/PM</td>
<td>2026-06-28</td>
</tr>
<tr>
<td>[ARCHITECTURE.md](../foundation/ARCHITECTURE.md)</td>
<td>1 Fondasi</td>
<td>FINAL</td>
<td>Software Architect</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>[GLOSSARY.md](../foundation/GLOSSARY.md)</td>
<td>1 Fondasi</td>
<td>FINAL</td>
<td>Domain Modeler/Tech Writer</td>
<td>2026-06-28</td>
</tr>
<tr>
<td>ROLES_AND_[PERMISSIONS.md](../foundation/ROLES_AND_PERMISSIONS.md)</td>
<td>1 Fondasi</td>
<td>FINAL</td>
<td>IAM/AuthZ Architect</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>STATUS_STATE_[MACHINE.md](../foundation/STATUS_STATE_MACHINE.md)</td>
<td>1 Fondasi</td>
<td>FINAL v1.3</td>
<td>Systems Analyst</td>
<td>2026-07-14</td>
</tr>
<tr>
<td>BUSINESS_[RULES.md](../foundation/BUSINESS_RULES.md)</td>
<td>1 Fondasi</td>
<td>FINAL</td>
<td>Business Analyst</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>MODULE_[CANDIDATES.md](../modules/MODULE_CANDIDATES.md)</td>
<td>2 Modul</td>
<td>FINAL</td>
<td>Backend Module Lead</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>MODULE_[JOBS.md](../modules/MODULE_JOBS.md)</td>
<td>2 Modul</td>
<td>FINAL</td>
<td>Backend Module Lead</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>MODULE_[PLACEMENT.md](../modules/MODULE_PLACEMENT.md)</td>
<td>2 Modul</td>
<td>FINAL</td>
<td>Backend Module Lead</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>MODULE_GUEST_[ACCESS.md](../modules/MODULE_GUEST_ACCESS.md)</td>
<td>2 Modul</td>
<td>FINAL</td>
<td>Backend + AppSec</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>MODULE_[AUTH.md](../modules/MODULE_AUTH.md)</td>
<td>2 Modul</td>
<td>FINAL</td>
<td>Security/IAM Engineer</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>MODULE_LOOKUP_[DATA.md](../modules/MODULE_LOOKUP_DATA.md)</td>
<td>2 Modul</td>
<td>FINAL</td>
<td>Backend/Data Modeler</td>
<td>2026-06-29</td>
</tr>
<tr>
<td>DATABASE_[SCHEMA.md](../technical/DATABASE_SCHEMA.md)</td>
<td>3 Teknis</td>
<td>FINAL v1.0</td>
<td>Database Architect</td>
<td>2026-07-14</td>
</tr>
<tr>
<td>UI_WIREFRAME_[NOTES.md](../ui/UI_WIREFRAME_NOTES.md)</td>
<td>3 Teknis</td>
<td>FINAL v1.0</td>
<td>UX/Frontend Architect</td>
<td>2026-07-14</td>
</tr>
<tr>
<td>DATA_RETENTION_AND_[PRIVACY.md](../technical/DATA_RETENTION_AND_PRIVACY.md)</td>
<td>4 Operasional</td>
<td>FINAL v1.2</td>
<td>Privacy/Compliance (DPO)</td>
<td>2026-07-14</td>
</tr>
<tr>
<td>BACKUP_AND_[RECOVERY.md](../technical/BACKUP_AND_RECOVERY.md)</td>
<td>4 Operasional</td>
<td>FINAL v1.0</td>
<td>DevOps/SRE</td>
<td>2026-07-13</td>
</tr>
<tr>
<td>[DEPLOYMENT.md](../technical/DEPLOYMENT.md)</td>
<td>4 Operasional</td>
<td>FINAL v1.0</td>
<td>DevOps/Infra</td>
<td>2026-07-13</td>
</tr>
<tr>
<td>API_[CONTRACTS.md](../technical/API_CONTRACTS.md)</td>
<td>4 Operasional</td>
<td>FINAL v1.2</td>
<td>API Designer</td>
<td>2026-07-14</td>
</tr>
<tr>
<td>SECURITY_[CHECKLIST.md](../technical/SECURITY_CHECKLIST.md)</td>
<td>4 Operasional</td>
<td>FINAL v1.2</td>
<td>AppSec Engineer</td>
<td>2026-07-14</td>
</tr>
</table>
---
## Log keputusan
Format entri:
### YYYY-MM-DD —  —
- Keputusan: …
- Alasan: …
- Affects: \[daftar file lintas-kelompok yang terdampak, mis. K1-ROLES, K2-AUTH, K4-SECURITY; atau “—”\]
- SUPERSEDES: \[entri lama yang ditimpa, atau “—”\]. Jika menimpa: tandai entri lama usang + butuh approval user.
- Perlu update PRD? (Ya/Tidak) …
---
### 2026-06-28 — PROJECT_[OVERVIEW.md](../foundation/PROJECT_OVERVIEW.md) — Taksonomi & penamaan modul (dwi-label)
- Keputusan: Modul memakai pemetaan dwi-label nama file (Inggris) ⇄ nama PRD (Indonesia). Eksplisit: **Jobs (MODULE_**[**JOBS.md**](../modules/MODULE_JOBS.md)**) = Modul Wawancara PRD**. Auth (MODULE_[AUTH.md](../modules/MODULE_AUTH.md)) & Lookup Data (MODULE_LOOKUP_[DATA.md](../modules/MODULE_LOOKUP_DATA.md)) adalah pecahan fungsional dari domain Super Admin/keamanan PRD; tidak ada satu "modul Super Admin" tunggal — Super Admin adalah role yang kewenangannya tersebar di Auth + Lookup + Audit (read-only).
- Alasan: Overview adalah pintu masuk semua agent; pemetaan eksplisit mencegah drift istilah antar-file. (K1, opsi C)
- Affects: K1-GLOSSARY, K1-ROLES_AND_PERMISSIONS, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_GUEST_ACCESS, K2-MODULE_AUTH, K2-MODULE_LOOKUP_DATA
- SUPERSEDES: —
- Perlu update PRD? Tidak (pemetaan penamaan, tidak mengubah kebenaran inti PRD).
### 2026-06-28 — PROJECT_[OVERVIEW.md](../foundation/PROJECT_OVERVIEW.md) — Penanganan 3 item terbuka PRD §11
- Keputusan: Overview mencantumkan nilai default PRD §11 dengan tanda `[ASUMSI — perlu konfirmasi user]`, bukan menebak nilai final. Default: volume kandidat 500–3.000; user internal ±15; retensi PII \~5–7 thn aktif + anonimisasi 1 thn pasca-final (TBD).
- Alasan: Tidak memblokir progres fondasi sambil tetap transparan; mudah di-finalize saat user konfirmasi. (K2, opsi B)
- Affects: K1-ARCHITECTURE (skalabilitas), K3-DATABASE_SCHEMA, K4-DATA_RETENTION_AND_PRIVACY, K4-DEPLOYMENT
- SUPERSEDES: —
- Perlu update PRD? Tidak (masih default PRD; perlu konfirmasi user untuk nilai final).
### 2026-06-28 — PROJECT_[OVERVIEW.md](../foundation/PROJECT_OVERVIEW.md) — Frontend belum dikunci di overview
- Keputusan: Overview mengunci backend/infra (Laravel 13.x · PHP 8.4 · PostgreSQL 18.x · Cloudflare R2 · VPS Lighthouse 2vCPU/2GB Tokyo · queue driver database), namun frontend ditandai `[BELUM DIPUTUSKAN]` dan diserahkan ke UI_WIREFRAME_[NOTES.md](../ui/UI_WIREFRAME_NOTES.md). Versi backend terverifikasi live 2026-06-28 ke sumber resmi ([php.net](http://php.net), [laravel.com](http://laravel.com), [postgresql.org](http://postgresql.org)); cocok dgn TECH_VERSION_SEED.
- Alasan: Disiplin scope — overview tidak boleh mendahului keputusan milik file UI. (K3, opsi A)
- Affects: K3-UI_WIREFRAME_NOTES
- SUPERSEDES: —
- Perlu update PRD? Tidak.
### 2026-06-28 — [GLOSSARY.md](../foundation/GLOSSARY.md) — Definisi kanonik istilah domain (final)
- Keputusan: [GLOSSARY.md](../foundation/GLOSSARY.md) difinalkan sebagai sumber tunggal definisi kanonik istilah domain. Istilah kunci yang dikunci: (1) "Job" ≡ Modul Wawancara PRD (bukan "lowongan/pekerjaan"); (2) Tamu = akses bertoken read-only, BUKAN akun/role internal (muncul di tabel §4.1 hanya sebagai konteks peta akses); (3) "Penempatan" (proses) dipisah dari "Kontainer Penempatan" (entitas); (4) "Maker–Checker" = bentuk kanonik, "Maker-Approval" menjadi alias usang; (5) "Force-Majeur" = ejaan proyek kanonik, selaras audit `FORCE_MAJEUR_ADDED` (ejaan umum: force majeure); (6) status bernama sama lintas state machine (`Mengundurkan Diri`/`Dikeluarkan`) WAJIB ber-qualifier `status_wawancara.X` vs `status_penempatan.X`; (7) glyph JP kanonik terverifikasi (歳, 男/女, 既婚/未婚, 右/左, 有り/無し, YYYY年MM月DD日) dengan aturan "store canonical enum, render glyph"; (8) lookup bilingual `label_id/label_ja/code`, perusahaan `nama_ja` wajib. Typo PRD Lampiran B.5 "DitolakDitolak" diasumsikan `Ditolak`.
- Alasan: Mengunci satu bahasa bersama mencegah ambiguitas & duplikasi konsep antar-agent. Sesuai rekomendasi & disetujui user 2026-06-28.
- Affects: K1-ROLES_AND_PERMISSIONS, K1-STATUS_STATE_MACHINE, K1-BUSINESS_RULES, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_GUEST_ACCESS, K2-MODULE_AUTH, K2-MODULE_LOOKUP_DATA, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES
- SUPERSEDES: —
- Perlu update PRD? Tidak (typo B.5 "DitolakDitolak" → "Ditolak" sebaiknya dikoreksi saat revisi PRD berikutnya; tidak mengubah kebenaran inti).
---
### 2026-06-29 — [ARCHITECTURE.md](../foundation/ARCHITECTURE.md) — Arsitektur modular monolith (D1–D10) final
- Keputusan: [ARCHITECTURE.md](../foundation/ARCHITECTURE.md) difinalkan. (D1) Modularisasi via Composer path repositories `internachi/modular` 2.x (`app-modules/`), bukan nwidart. (D2) Komunikasi antar-modul = public service/facade sinkron + internal domain events sinkron in-process (tanpa Redis); larangan akses tabel/Model & FK lintas-modul (PRD §9.7); ketersediaan kandidat hanya via service publik Candidates (PRD §7.1). (D3) Layering 4 lapis (Domain/Application/Infrastructure/Presentation); business rules di Domain+Application. (D4) Audit log = service custom ke tabel `audit_log` JSONB sesuai PRD Lampiran A, immutable, Super Admin read-only. (D5) Queue driver=database + 1 worker Supervisor; hanya email kritis di-queue; anti-duplikasi via unique constraint+transaksi (tanpa Redis lock). (D6) Notifikasi in-app + polling ≤60 dtk, tanpa websocket. (D7) File di R2 (Flysystem s3) bucket privat+SSE, signed URL 5–15 mnt, envelope encryption dok identitas; caveat checksum aws-sdk-php `WHEN_REQUIRED` wajib diuji. (D8) Konkurensi: optimistic `version` lintas agregat + pessimistic `SELECT FOR UPDATE` khusus pull bulk + verifikasi `pending_request` di transaksi (PRD §7.10). (D9) i18n "store canonical enum, render glyph" (PRD §9.4). (D10) Error handling (409/403/422/410), feature isolation, testing seams. Tambahan: penomoran §PRD pada brief agent dikoreksi agar akurat (audit=Lampiran A/§5.1/§7.11; kontrak=§9.7; konkurensi=§7.10) — koreksi rujukan, bukan perubahan isi PRD.
- Alasan: Modular monolith ramping yang MUAT di VPS 2vCPU/2GB tanpa Redis/websocket (PRD §9.6); semua klaim tech terverifikasi live 2026-06-29; menutup GAP-1..GAP-4. Disetujui user 2026-06-29.
- Affects: K1-BUSINESS_RULES, K1-STATUS_STATE_MACHINE, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_GUEST_ACCESS, K2-MODULE_AUTH, K2-MODULE_LOOKUP_DATA, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY, K4-DEPLOYMENT, K4-BACKUP_AND_RECOVERY
- SUPERSEDES: —
- Perlu update PRD? Tidak (selaras PRD v0.3; koreksi nomor §PRD hanya menyangkut brief agent, bukan isi PRD).
### 2026-06-29 — ROLES_AND_[PERMISSIONS.md](../foundation/ROLES_AND_PERMISSIONS.md) — Model peran & izin (D-R1..D-R7, GAP-R4/R5) final
- Keputusan: ROLES_AND_[PERMISSIONS.md](../foundation/ROLES_AND_PERMISSIONS.md) difinalkan. (D-R1) Enforcement hybrid: paket role-assignment (spatie/laravel-permission 8.1) + Policy untuk scope/kepemilikan. (D-R2) Izin = permission bernama di-seed, dipetakan ke 6 role hardcode (bukan cek per-role mentah). (D-R3) 2FA login first-party (Laravel Fortify, TOTP RFC 6238) + step-up re-auth custom (password+TOTP per aksi; tidak native di Fortify — \[PERLU VERIFIKASI implementasi\], detail → MODULE_AUTH). (D-R4) Scope global untuk MVP + jejak dibuat_oleh/disetujui_oleh; scoping kepemilikan ketat ditunda post-MVP. (D-R5) Hard-block kombinasi peran yang melanggar SoD (Staf Input+Approver Kandidat; Asisten Manajer+Manajer Job; Super Admin+operasional; internal+Tamu); default 1 user=1 role. (D-R6) 2FA wajib hanya 3 role (Approver Kandidat, Manajer Job, Super Admin) sesuai PRD §4.5; wajib-untuk-semua = catatan keamanan opsional. (D-R7) Tamu = aktor token terpisah di luar RBAC internal, hanya baca GuestCandidateView. (GAP-R4) Hanya Super Admin lihat log audit pusat; role lain cukup riwayat dalam kontainer. (GAP-R5) Tanpa fitur pendelegasian approver di MVP; Super Admin assign/unassign role manual bila penyetuju berhalangan. Temuan: §7.9 (anonimisasi PII butuh Super Admin+step-up) menambah pemicu step-up ke-5 di luar 4 kategori Lampiran D.
- Alasan: Model izin aman & enforceable; least-privilege + separation of duties; 100% selaras PRD v0.3 (6 role hardcode, TOTP 3 role, step-up 4 aksi). Tech terverifikasi live 2026-06-29 (spatie/laravel-permission 8.1.0; Laravel Fortify untuk Laravel 13.x). Disetujui user 2026-06-29.
- Affects: K2-MODULE_AUTH, K2-MODULE_GUEST_ACCESS, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_LOOKUP_DATA, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY, K4-API_CONTRACTS
- SUPERSEDES: —
- Perlu update PRD? Tidak untuk kebenaran inti. Usulan minor untuk revisi PRD berikutnya: perjelas Lampiran D agar mencantumkan anonimisasi PII (§7.9) sebagai pemicu step-up ke-5.
---
### 2026-06-29 — STATUS_STATE_[MACHINE.md](../foundation/STATUS_STATE_MACHINE.md) — Penguncian 7 mesin status (final)
- Keputusan: STATUS_STATE_[MACHINE.md](../foundation/STATUS_STATE_MACHINE.md) difinalkan. Tujuh mesin status dikunci lengkap (tabel transisi Dari→Ke · pemicu · aktor · guard · efek samping/audit · Approval/Step-up, plus status terminal & transisi terlarang eksplisit): Kontainer Wawancara (5), Kontainer Penempatan (5), status_wawancara (8), status_penempatan (4), Approval Kandidat (4 + terminal internal `Diterapkan`), Link Tamu (3), dan transisi khusus Force-Majeur. Keputusan GAP: (GAP-1) "status aktif" status_wawancara = \{Menunggu Wawancara, Lulus, Proses Dokumen, Siap Dikirim\}; Terkirim dikecualikan/terminal. (GAP-2) link tamu ditolak = tanpa record `guest_link`, token tak lahir (penolakan di pending_request). (GAP-3) freeze partisipasi saat kontainer wawancara Ditutup = guard turunan (blok semua transisi status_wawancara), availability→Tersedia, tanpa status baru. (GAP-4) \[GAP-PRD\] escape `Aktif→Dibatalkan` kontainer penempatan hanya bila count(placement_participant)=0, ber-approval, untuk cegah deadlock kontainer Aktif kosong. (GAP-5) `Siap Dikirim→Terkirim` hanya efek samping approval batch kirim Penempatan (cross-module), tanpa aksi manual di modul Wawancara. (GAP-6) baris draft-revisi kandidat yang disetujui → status terminal internal `Diterapkan` (merged), data utama tetap Disetujui. \[ASUMSI\] Force-Majeur tanpa step-up (selaras Lampiran D PRD). Enforcement: spatie/laravel-model-states + CHECK constraint DB + optimistic `version`/SELECT FOR UPDATE.
- Alasan: Mengunci enforcement transisi yang konsisten & tanpa celah agar DATABASE_SCHEMA dan modul dapat menegakkannya seragam; selaras PRD v0.3 + GLOSSARY (final) + ARCHITECTURE D8. Tech enforcement terverifikasi live 2026-06-29 (spatie/laravel-model-states 2.14.1, Laravel 13/PHP 8.4). Disetujui user 2026-06-29.
- Affects: K1-BUSINESS_RULES, K1-GLOSSARY, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_GUEST_ACCESS, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST
- SUPERSEDES: —
- Perlu update PRD? Ya (minor) — **SUDAH DITERAPKAN ke PRD (judul kini PRD_Kakehashi_v0_3_4), disetujui user 2026-06-29:** (1) GAP-4 escape `Aktif→Dibatalkan` (guard `count(placement_participant)=0`, ber-approval Manajer Job) telah dicantumkan di **§7.6 & Lampiran B.2** (diterapkan via revisi PRD v0.3.3); (2) Force-Majeur ditegaskan **TIDAK** memicu step-up re-auth di **§6.4 Sub-flow 2b & Lampiran D** (diterapkan via revisi PRD v0.3.2). Kedua usulan update PRD dari STATUS_STATE_MACHINE kini **tertutup**; tidak ada item PRD terbuka tersisa dari mesin status.
---
### 2026-06-29 — BUSINESS_[RULES.md](../foundation/BUSINESS_RULES.md) — Aturan bisnis fondasi (BR-APV/FM/DUP/NIK/TGT/CON/PII/I18N) final
- Keputusan: BUSINESS_[RULES.md](../foundation/BUSINESS_RULES.md) difinalkan berisi aturan atomik & testable ber-ID untuk 8 kategori. Keputusan kunci yang dipilih user: (A2) cek-kemiripan dihitung pada nama latin DAN katakana (match bila salah satu similarity ≥ 0,4) + tanggal lahir sama persis + kewarganegaraan sama, mencakup data draft, mengecualikan kandidat ter-anonimisasi; (B1) perilaku saat match = soft warning + konfirmasi eksplisit (TIDAK memblokir), override dicatat audit; (C1) Nomor Induk `K-YYYY-NNNNN` di-generate saat submit pertama, penghitung per-tahun mulai 1, YYYY = tahun zona JST (Asia/Tokyo), boleh ada gap, keunikan dijamin unique constraint & permanen; (D1) Sub-flow Force-Majeur (Tersedia+Disetujui, ref_riwayat_partisipasi=null, alasan wajib, approval Manajer Job, operasi atomik) TANPA step-up re-auth, audit dua lapis `FORCE_MAJEUR_ADDED`; (E1) PII — definisikan mekanisme (pisah catatan operasional permanen vs PII; anonimisasi via soft tombstone; hanya Super Admin + step-up ke-5; audit `CANDIDATE_ANONYMIZED`; PII anonim tak bisa diedit/dipulihkan), periode retensi ditandai \[ASUMSI §11\]. Aturan lain dikunci: Maker–Checker (pemisahan, catatan tolak wajib, blokir resubmit tanpa perubahan, anti double-approval via verifikasi `pending` di dalam transaksi → 409), penolakan kontainer wawancara → Draft (rujuk STATUS_STATE_MACHINE), target peserta diterima = informatif/soft warning (tidak block), konkurensi optimistic `version`→409 + pessimistic FOR UPDATE khusus bulk pull, i18n `nama_ja` perusahaan wajib + simpan enum kanonik render glyph. Ambang 0,4 ditegaskan deterministik (`similarity() >= 0.4` eksplisit, bukan operator `%`/ambang global).
- Alasan: Aturan konsisten dengan PRD v0.3 + GLOSSARY (final) + STATUS_STATE_MACHINE (final) + ARCHITECTURE D8/D10; atomik, testable, tanpa kontradiksi. Tech (pg_trgm similarity/GIN, sequence atomik + sifat gap, optimistic/pessimistic locking) terverifikasi live ke dokumentasi resmi PostgreSQL 18 pada 2026-06-29. Disetujui user 2026-06-29.
- Affects: K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY
- SUPERSEDES: —
- Perlu update PRD? Ya (minor, selaras usulan STATUS_STATE_MACHINE): (1) tegaskan di Lampiran D / §6.4 bahwa Force-Majeur TIDAK memicu step-up (keputusan D1); (2) §11 — konfirmasi periode retensi PII (BR-PII-06 masih \[ASUMSI\]). Tidak ada perubahan kebenaran inti PRD.
---
### 2026-06-29 — MODULE_[AUTH.md](../modules/MODULE_AUTH.md) — Fondasi autentikasi (A-1..A-7) final
- Keputusan: MODULE_[AUTH.md](../modules/MODULE_AUTH.md) difinalkan (draft final-ready, disetujui user 2026-06-29). (A-1) Daftar aksi pemicu step-up MENGIKUTI PRD §4.6/Lampiran D + ROLES §8.2 (final), BUKAN daftar di brief misi: yang memicu step-up = (1) ubah role/nonaktifkan akun, (2) setujui penutupan kontainer wawancara, (3) setujui pengeluaran kandidat, (4) kelola lookup/config + master perusahaan, (5) anonimisasi PII (§7.9); Approve/Reject kandidat = approval rutin → TIDAK memicu step-up. (A-2) Library TOTP = Laravel Fortify (first-party, TOTP RFC 6238 + recovery codes bawaan), bukan pragmarx/google2fa. (A-3) Hashing password = bcrypt cost 12 (bukan argon2id) demi efisiensi RAM di VPS 2vCPU/2GB. (A-4) Lockout = 5 gagal berturut → kunci 15 menit (key email+IP, RateLimiter driver database). (A-5) Recovery codes = 8, single-use, terenkripsi at-rest (cast `encrypted` AES-256 via APP_KEY), regen meng-invalidasi semua kode lama. (A-6) Step-up = re-auth password+TOTP, TTL 5 menit, per-aksi (middleware kustom; `password.confirm` Fortify TIDAK dipakai karena password-only & sesi 3 jam). (A-7) Password policy = min 12 char + wajib 3 dari 4 kelas karakter. Session: driver database, idle 30 mnt (PRD §4.4), regenerate pada login & elevasi step-up. SSO/OAuth dikecualikan (PRD §4.5). Koreksi rujukan: TOTP wajib di PRD §4.5 (bukan §9.1), step-up di §4.6/Lampiran D (bukan §9.2).
- Alasan: Aman, audit-friendly, dan muat di VPS kecil tanpa Redis; selaras PRD v0.3 + ROLES (final, D-R3/D-R6) + GLOSSARY (final) + BUSINESS_RULES (audit). Tech terverifikasi live ke sumber resmi 2026-06-29 (Laravel 13.x, PHP 8.4, Fortify bawaan, spatie/laravel-permission \^8.1, encrypted cast AES-256-CBC). Disetujui user 2026-06-29.
- Affects: K1-ROLES_AND_PERMISSIONS, K1-BUSINESS_RULES, K2-MODULE_GUEST_ACCESS, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_LOOKUP_DATA, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-SECURITY_CHECKLIST, K4-API_CONTRACTS, K4-DATA_RETENTION_AND_PRIVACY
- SUPERSEDES: —
- Perlu update PRD? Ya (minor, audit): tambahkan ke enum action_type Auth (Lampiran A) event granular `LOGIN_LOCKED_OUT`, `TWOFA_VERIFIED`, `TWOFA_FAILED`, `TWOFA_RECOVERY_USED`, dan `STEPUP_FAILED` (atau encode hasil gagal di `detail.result`) — non-breaking. **SUDAH DITERAPKAN ke PRD v0.3.1 (29-06-2026, disetujui user)**: kelima type ditambahkan ke enum `action_type` domain Auth (Lampiran A); sekaligus anonimisasi PII (§7.9) ditegaskan sebagai pemicu step-up re-auth ke-5 di §4.6 & Lampiran D. Tidak ada perubahan kebenaran inti PRD.
---
### 2026-06-29 — PRD_Kakehashi (v0.3.1 → v0.3.2) — Klarifikasi Force-Majeur tanpa step-up
- Keputusan: PRD dinaikkan ke **v0.3.2** (judul dokumen telah berubah dari PRD_Kakehashi_v0_3 → v0_3_1 via adendum MODULE_AUTH, dan kini → v0_3_2). Menegaskan secara eksplisit di §6.4 Sub-flow 2b dan Lampiran D bahwa Sub-flow Force-Majeur (Tambah Langsung) **TIDAK** memicu step-up re-auth — kontrol cukup approval Manajer Job + alasan wajib + audit dua lapis `FORCE_MAJEUR_ADDED`. Ini menutup usul update PRD dari BUSINESS_RULES (D1) & STATUS_STATE_MACHINE terkait force-majeur.
- Alasan: Menghapus ambiguitas antara frasa "gaya EXPEL" di §6.4 dan Lampiran D yang tidak mendaftarkan force-majeur sebagai pemicu step-up. Non-breaking, selaras keputusan D1 BUSINESS_RULES. Disetujui user 2026-06-29.
- Affects: K1-BUSINESS_RULES, K1-STATUS_STATE_MACHINE, K2-MODULE_PLACEMENT, K2-MODULE_AUTH, K4-SECURITY_CHECKLIST
- SUPERSEDES: — (tidak menimpa keputusan final; melengkapi usul update PRD pada entri BUSINESS_RULES & STATUS_STATE_MACHINE 2026-06-29)
- Perlu update PRD? SUDAH DITERAPKAN ke PRD v0.3.2 (29-06-2026, disetujui user). Catatan terbuka: §11 (periode retensi PII, BR-PII-06) MASIH menunggu angka final dari user — belum ditutup; serta usulan GAP-4 STATUS_STATE_MACHINE (escape `Aktif→Dibatalkan` di §7.6/Lampiran B.2) belum diterapkan ke PRD.
---
### 2026-06-29 — PRD_Kakehashi (v0.3.2 → v0.3.3) — Retensi PII final + sinkronisasi GAP-4
- Keputusan: PRD dinaikkan ke **v0.3.3**. (1) **Retensi PII (§11) dikunci**: simpan PII **5 tahun** aktif sejak keterikatan terakhir kandidat, lalu **anonimisasi** (soft tombstone) dalam tenggang **≤ 1 tahun**; rincian jadwal/prosedur didelegasikan ke DATA_RETENTION_AND_[PRIVACY.md](../technical/DATA_RETENTION_AND_PRIVACY.md) (pending konfirmasi DPO). (2) **GAP-4 disinkronkan** ke PRD §7.6 & Lampiran B.2: escape transition `Aktif→Dibatalkan` kontainer penempatan hanya bila `count(placement_participant)=0` + ber-approval Manajer Job. BUSINESS_RULES BR-PII-06 diperbarui dari \[ASUMSI\] menjadi DITETAPKAN.
- Alasan: Retensi 5 thn selaras praktik retensi catatan ketenagakerjaan Jepang (LSA) + asas minimalisasi data APPI/UU PDP; GAP-4 mencegah deadlock kontainer Aktif kosong dan menyinkronkan PRD dengan STATUS_STATE_MACHINE (final). Disetujui user 2026-06-29.
- Affects: K1-BUSINESS_RULES, K1-STATUS_STATE_MACHINE, K2-MODULE_PLACEMENT, K3-DATABASE_SCHEMA, K4-DATA_RETENTION_AND_PRIVACY
- SUPERSEDES: — (melengkapi item terbuka pada entri BUSINESS_RULES & STATUS_STATE_MACHINE 2026-06-29; tidak menimpa keputusan final)
- Perlu update PRD? SUDAH DITERAPKAN ke PRD v0.3.3 (29-06-2026). Catatan: angka retensi PII final tetap perlu konfirmasi DPO; dua item §11 lain (volume kandidat ±500–3.000, jumlah user ±15) masih TBD.
### 2026-06-29 — PRD_Kakehashi (v0.3.3 → v0.3.4) — Penutupan dua item terbuka §11 (kapasitas)
- Keputusan: Dua item terbuka terakhir di PRD §11 dikunci atas konfirmasi user: (1) estimasi volume kandidat 1–3 tahun = **500–3.000**; (2) jumlah user internal total = **±15 user**. Keduanya ditetapkan sebagai angka perencanaan kapasitas (bukan batas keras) dan dapat ditinjau ulang sesuai realita operasional. Disinkronkan ke PRD §9.3 (Skalabilitas) dan §11. Seluruh tiga item §11 kini tertutup (retensi PII dikunci di v0.3.3, rincian jadwal menunggu DPO).
- Alasan: Angka selaras kapasitas yang sudah diasumsikan sejak fondasi (±25 concurrent users, 500–3.000 kandidat) dan tidak mengubah keputusan teknis manapun; menutup §11 agar PRD dapat menuju final. Disetujui user 2026-06-29.
- Affects: K1-ARCHITECTURE, K3-DATABASE_SCHEMA, K4-DEPLOYMENT, K4-BACKUP_AND_RECOVERY
- SUPERSEDES: — (melengkapi entri 2026-06-28 PROJECT_OVERVIEW "Penanganan 3 item terbuka PRD §11"; nilai yang semula asumsi kini berstatus dikunci)
- Perlu update PRD? SUDAH DITERAPKAN ke PRD v0.3.4 (29-06-2026). Catatan: hanya rincian jadwal retensi PII yang masih menunggu konfirmasi DPO (DATA_RETENTION_AND_[PRIVACY.md](../technical/DATA_RETENTION_AND_PRIVACY.md)); seluruh item §11 lainnya tertutup.
---
### 2026-06-29 — MODULE_LOOKUP_[DATA.md](../modules/MODULE_LOOKUP_DATA.md) — Master data lookup, canonical enum & rendering bilingual (D-L1..D-L6) final
- Keputusan: MODULE_LOOKUP_[DATA.md](../modules/MODULE_LOOKUP_DATA.md) difinalkan (draft final-ready, disetujui user 2026-06-29). (D-L1) **Caching tanpa Redis** = driver `database` (default Laravel 13) + cache key per-tabel per-bahasa (`lookup:{table}:{lang}`, TTL 24 jam) + `Cache::forget()` on write; **cache tags TIDAK dipakai** karena tidak didukung driver `file`/`database`/`array` (hanya Redis/Memcached) — terverifikasi live ke [laravel.com/docs/13.x/cache](http://laravel.com/docs/13.x/cache) 2026-06-29. (D-L2) **Format ****`code`**** kanonik = hybrid ISO**: `negara` = ISO 3166-1 alpha-2, `bahasa` = ISO 639-1, tabel lain = SCREAMING_SNAKE_CASE. (D-L3) **Hybrid enum vs lookup table**: nilai fixed ber-glyph (Jenis Kelamin `M`/`F`→男/女, Status Pernikahan `MARRIED`/`SINGLE`→既婚/未婚, Dominan Tangan `RIGHT`/`LEFT`→右/左, boolean fisik `YES`/`NO`→有り/無し, Jenis Wawancara `OFFLINE`/`ONLINE`, satuan umur 歳) = **PHP 8.4 backed enum** (hardcode + CHECK constraint); daftar terbuka admin-editable = **DB lookup table** bilingual (`code`/`label_id`/`label_ja`). (D-L4) **Skill SSW = satu tabel** `skill_ssw` + kolom `bidang_id` (terkategori per bidang), bukan tabel per-bidang. (D-L5) **Seed geografi** = provinsi Indonesia lengkap (38); kota/kabupaten & kecamatan sampel di MVP, dilengkapi impor data Kemendagri pasca-MVP. (D-L6) **CRUD lookup mengikuti ROLES §5.5 (tidak diubah)**: Super Admin buat/nonaktifkan lookup + setujui request + kelola master perusahaan, semua dengan 🔒 step-up re-auth; Staf Input & Asisten Manajer hanya mengajukan request. Skema umum tiap tabel Kelas 2 = `id, code (unik, non-empty, immutable), label_id (wajib), label_ja (wajib), sort_order, is_active (soft-disable, bukan hard-delete bila dirujuk), created_at, updated_at` + kolom tambahan per-tabel. Validasi: regex `code` (`^[A-Z]{2}$` negara, `^[a-z]{2}$` bahasa, `^[A-Z0-9_]+$` lainnya); flag `is_shareable` pada sertifikat skill untuk whitelist Tamu (default false). Rendering: helper `lookup_label($table,$code,$lang)` + Blade directive `@lookup` + Filament 5 `Select::options()` terlokalisasi, fallback berjenjang ke bahasa lain lalu `code` mentah; Tamu = JP eksplisit. Audit memakai event Lampiran A (`LOOKUP_CREATED/DEACTIVATED/REQUEST_SUBMITTED/APPROVED/REJECTED`, `COMPANY_*`).
- Alasan: Master data konsisten, bilingual, dan ringan di VPS 2vCPU/2GB tanpa Redis; selaras PRD v0.3.4 (§5.2–5.4, §7.8, §9.4, Lampiran A/C) + GLOSSARY (final, store canonical enum render glyph) + ROLES_AND_PERMISSIONS (final §5.5) + ARCHITECTURE (D4 audit, D9 i18n). Tech terverifikasi live 2026-06-29 (Laravel 13.x, PHP 8.4 backed enums, driver cache database, Filament 5.x/Livewire 4.x). Disetujui user 2026-06-29.
- Affects: K1-ROLES_AND_PERMISSIONS, K1-GLOSSARY, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_GUEST_ACCESS, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST
- SUPERSEDES: —
- Perlu update PRD? Tidak untuk kebenaran inti. Koreksi rujukan minor: aturan canonical enum + glyph ada di **PRD §9.4** (bukan §9.6 seperti tertulis di brief misi). GAP terbuka: konten seed final beberapa lookup (golongan darah, ukuran sepatu, tingkat penglihatan, agama, status keluarga, jenis_visa, asal rekrutmen, bidang diminati) menunggu konfirmasi kumiai; daftar status tinggal Jepang perlu validasi legal. Pertanyaan terbuka **Q1** (tabel `bahasa` mandiri) & **Q2** (tempat lahir mendukung multi-negara, seed fokus Indonesia) telah **DITUTUP** atas keputusan user 2026-06-29.
---
### 2026-06-29 — MODULE_[CANDIDATES.md](../modules/MODULE_CANDIDATES.md) — Domain Kandidat (6 keputusan GATE) final
- Keputusan: MODULE_[CANDIDATES.md](../modules/MODULE_CANDIDATES.md) difinalkan (draft final-ready, disetujui user 2026-06-29). Enam keputusan terbuka diputuskan user, SEMUANYA selaras dependency final: (1) **Tamu read-only TANPA upload** sertifikat (ikuti ROLES §10/D-R7 & PRD §4.3; brief misi "Tamu hanya skill cert upload" ditolak). (2) **Bulk pull oleh Asisten Manajer**; **Manajer Job TIDAK punya akses modul Kandidat** (ikuti ROLES §5.1 & STATUS_STATE_MACHINE; brief "Manajer Job baca/edit & bulk pull" ditolak). (3) **Envelope encryption manual AES-256-GCM** per-file (DEK acak/file dibungkus KEK; KEK di luar repo, env/secret VPS; rotasi via `kek_version`); tanpa AWS KMS & tanpa paket pihak ketiga. (4) **Signed URL R2 bertingkat**: dokumen identitas 5 menit, foto 15 menit. (5) **Optimistic locking manual kolom ****`version`**** → HTTP 409** (selaras ARCHITECTURE D8 + BR-CON-01/02); tanpa paket pihak ketiga. (6) **Cek-kemiripan pg_trgm ****`similarity() >= 0.4`**** eksplisit** pada nama latin & katakana apa adanya + DOB exact + kewarganegaraan exact, mencakup DRAFT, kecualikan anonim, GIN `gin_trgm_ops`, soft warning (tak memblokir) sesuai BR-DUP. Modul juga mengunci: NIK `K-YYYY-NNNNN` assign saat submit via counter/SEQUENCE per-tahun (zona JST) + unique constraint transaksional (BR-NIK); pemisahan kolom operasional vs PII + retensi PII 5 thn + anonimisasi ≤1 thn (PRD §7.9/§9.8/§11, v0.3.3); pessimistic `SELECT FOR UPDATE` khusus bulk pull (BR-CON-03); step-up re-auth HANYA untuk anonimisasi PII (bukan soft delete biasa).
- Alasan: Selaras penuh PRD v0.3.4 + GLOSSARY + ROLES + STATUS_STATE_MACHINE + BUSINESS_RULES + MODULE_LOOKUP_DATA + ARCHITECTURE (D2/D4/D8/D9). Tech terverifikasi live ke sumber resmi 2026-06-28/29 (PostgreSQL 18 pg_trgm `similarity()`/GIN, Laravel 13/PHP 8.4, Flysystem v3 + aws-sdk-php checksum `WHEN_REQUIRED` + `retain_visibility=false`, OpenSSL AES-256-GCM, intervention/image 3, spatie/laravel-model-states 2.14.1). Disetujui user 2026-06-29.
- Affects: K2-MODULE_PLACEMENT, K2-MODULE_GUEST_ACCESS, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY
- SUPERSEDES: — (tidak menimpa keputusan final; keputusan #1 & #2 menyelaraskan brief misi ke dependency final yang sudah ada, bukan menimpa entri lama)
- Perlu update PRD? Tidak untuk kebenaran inti. Koreksi rujukan §PRD pada brief misi (bukan perubahan isi): atribut kandidat = §5.2 (bukan "§7 Tabel 6"); Force-Majeur = §6.4 Sub-flow 2b (bukan §9.3); canonical enum+glyph = §9.4 (bukan §9.6). **SUDAH DITERAPKAN ke PRD v0.3.5** (disetujui user 2026-06-29): ditambahkan ke enum `action_type` Lampiran A.1 domain Kandidat — `CANDIDATE_CREATED`, `CANDIDATE_SOFT_DELETED`, `CANDIDATE_RESTORED`, `IDENTITY_DOC_VIEWED`, `CANDIDATE_PHOTO_UPLOADED`, `SIMILARITY_MATCH_SHOWN` (+ contoh skema `detail` JSONB di Lampiran A.2). Lihat entri PRD v0.3.4 → v0.3.5 di bawah. GAP terbuka: seed beberapa lookup (agama, golongan darah, dll) menunggu konfirmasi kumiai; rincian jadwal retensi PII menunggu DPO.
---
### 2026-06-29 — PRD_Kakehashi (v0.3.4 → v0.3.5) — Adendum enumerasi audit domain Kandidat
- Keputusan: PRD dinaikkan ke **v0.3.5** (judul dokumen → PRD_Kakehashi_v0_3_5). Ditambahkan **6 ****`action_type`**** audit domain Kandidat** ke Lampiran A.1 — `CANDIDATE_CREATED` (draft kandidat dibuat), `CANDIDATE_SOFT_DELETED`, `CANDIDATE_RESTORED`, `IDENTITY_DOC_VIEWED` (akses dokumen identitas Zairyu/Paspor — WAJIB tercatat), `CANDIDATE_PHOTO_UPLOADED`, `SIMILARITY_MATCH_SHOWN` (peringatan kemiripan ditampilkan) — beserta contoh skema `detail` JSONB di Lampiran A.2. Menutup usul update PRD dari entri MODULE_[CANDIDATES.md](../modules/MODULE_CANDIDATES.md) 2026-06-29.
- Alasan: Brief misi MODULE_CANDIDATES mewajibkan audit candidate_create/update/soft_delete/restore/doc_view/photo_upload/similarity_match_shown, namun 6 di antaranya belum ada di enum Lampiran A. `IDENTITY_DOC_VIEWED` khususnya wajib demi jejak akses dokumen identitas sensitif (PRD §9.1) dan retensi PII (§7.9). Non-breaking, tidak mengubah kebenaran inti. Disetujui user 2026-06-29.
- Affects: K2-MODULE_CANDIDATES, K3-DATABASE_SCHEMA, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY, K4-API_CONTRACTS
- SUPERSEDES: — (melengkapi usul update PRD pada entri MODULE_[CANDIDATES.md](../modules/MODULE_CANDIDATES.md) 2026-06-29; tidak menimpa keputusan final)
- Perlu update PRD? SUDAH DITERAPKAN ke PRD v0.3.5 (29-06-2026, disetujui user). Catatan: skema `detail` lengkap per action_type didelegasikan ke BUSINESS_RULES / AUDIT_EVENTS turunan. GAP terbuka tak berubah: seed beberapa lookup menunggu konfirmasi kumiai; rincian jadwal retensi PII menunggu DPO.
---
### 2026-06-29 — MODULE_[JOBS.md](../modules/MODULE_JOBS.md) — Domain Kontainer Wawancara (Jobs) final
- Keputusan: MODULE_[JOBS.md](../modules/MODULE_JOBS.md) difinalkan (draft final-ready, disetujui user 2026-06-29). Lima keputusan GATE diputuskan user, SEMUANYA mengikuti rekomendasi & selaras dependency final: (1) **Step-up re-auth HANYA Tutup Kontainer (+ Keluarkan kandidat/expel)** — Approve/Reject/Cancel kontainer = approval rutin TANPA step-up (ikuti PRD §4.6/Lampiran D + ROLES §8.2 + MODULE_AUTH A-1; daftar brief "Approve/Reject/Close/Cancel" ditolak). (2) **Tanpa aksi approve/reject kandidat di modul Jobs** — Approver Kandidat hanya di MODULE_CANDIDATES (ROLES §5.1); alur kandidat dalam kontainer = tarik (Asisten Manajer) → update `status_wawancara` (jalur alami) → keluarkan/expel (approval Manajer Job + step-up). Event `candidate_approve_in_job`/`candidate_reject_in_job` TIDAK dibuat. (3) **Audit memakai enum kanonik PRD Lampiran A** (`IC_CREATED`, `IC_SUBMITTED`, `IC_APPROVED`, `IC_REJECTED`, `IC_CLOSE_REQUESTED`, `IC_CLOSED`, `IC_CANCELLED`, `CANDIDATE_PULLED`, `PARTICIPATION_STATUS_CHANGED`, `EXPEL_*`); nama brief `job_*` dipetakan; `job_activate` = efek `IC_APPROVED` (bukan event terpisah). (4) **Overlay "Menunggu Persetujuan Penutupan" = turunan dari ****`pending_request`**** CLOSE terbuka** (tanpa kolom status/flag tambahan); status tetap `Aktif` (STATUS_STATE_MACHINE §1). (5) **Form Kontainer tanpa autosave** — simpan Draft manual (status Draft = save point), konsisten dengan alur optimistic `version`. Modul juga mengunci: 5 status final + transisi terlarang eksplisit (enforcement spatie/laravel-model-states + CHECK + `version`); atribut kontainer dari PRD §5.3; `perusahaan.nama_ja` WAJIB, `nama_romaji`/`nama_id` OPSIONAL (final); Posisi Pekerjaan via lookup `posisi_pekerjaan`(+`bidang_pekerjaan_id`); target peserta = soft warning informatif (BR-TGT, tidak memblok); konkurensi optimistic `version`→409 (BR-CON-01/02) + pessimistic FOR UPDATE khusus bulk pull (BR-CON-03) + anti double-approval via verifikasi `pending_request` dalam transaksi; visibilitas Tamu hanya kontainer Aktif via GuestCandidateView (delegasi MODULE_GUEST_ACCESS); freeze partisipasi + markAvailable saat Ditutup (GAP-3).
- Alasan: Selaras penuh PRD v0.3.5 + GLOSSARY + ROLES + STATUS_STATE_MACHINE + BUSINESS_RULES + MODULE_LOOKUP_DATA + MODULE_AUTH + ARCHITECTURE (D2/D4/D8/D9). Tech terverifikasi live 2026-06-29 (Laravel 13.x, spatie/laravel-model-states 2.14.1, Filament 5.x ≥5.3.5/Livewire 4.x, optimistic lock pola manual `version`). Disetujui user 2026-06-29.
- Affects: K2-MODULE_CANDIDATES, K2-MODULE_GUEST_ACCESS, K2-MODULE_PLACEMENT, K2-MODULE_LOOKUP_DATA, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST
- SUPERSEDES: — (tidak menimpa keputusan final; keputusan #1 & #2 menyelaraskan brief misi ke dependency final yang sudah ada, bukan menimpa entri lama)
- Perlu update PRD? Tidak untuk kebenaran inti. Koreksi rujukan §PRD pada brief misi (bukan perubahan isi): atribut kontainer = §5.3 (bukan "§7 Tabel 7"); canonical enum+glyph = §9.4 (bukan §9.6). **GAP-J3 DITUTUP (keputusan user 2026-06-29):** dipilih **Opsi A** — peringatan target ditampilkan sebagai soft warning **tanpa** dicatat ke audit; tidak perlu enum baru & tidak ada update PRD. Tidak ada GAP terbuka tersisa untuk MODULE_JOBS — modul **FINAL** & siap handoff.
---
### 2026-06-29 — MODULE_[PLACEMENT.md](../modules/MODULE_PLACEMENT.md) — Domain Kontainer Penempatan final
- Keputusan: MODULE_[PLACEMENT.md](../modules/MODULE_PLACEMENT.md) difinalkan (disetujui user 2026-06-29). Lima keputusan GATE diputuskan user mengikuti rekomendasi & selaras dependency final: (1) **Step-up re-auth HANYA Cabut Penempatan** (`Dikeluarkan`/`PLACEMENT_EXPEL` setelah Aktif) — Approve/Reject/Cancel kontainer + approve Force-Majeur + approve Mengundurkan Diri = approval rutin TANPA step-up (ikuti PRD §4.6/Lampiran D + ROLES §8.2 + MODULE_AUTH A-1; daftar brief ditolak). (2) **Kolom referensi partisipasi wawancara asal = ****`source_participation_id`** (kanonik PRD §5.4); NULL ⇒ jalur Force-Majeur (brief `ref_riwayat_partisipasi` dipetakan). (3) **Auto-archive = sinkron in-transaction setelah batch (utama) + scheduled command harian (jaring pengaman)**; idempotensi via guard state machine + unique constraint DB, bukan lock Redis. (4) **Alasan Force-Majeur = kategori (lookup baru ****`kategori_force_majeur`****) + free-text, dua-duanya wajib** saat `source_participation_id` NULL (CHECK constraint). (5) **Batas bulk pull/batch kirim = maksimum 50 kandidat per operasi** (caveat VPS 2vCPU/2GB, durasi lock FOR UPDATE). Modul juga mengunci: 5 status kontainer (Draft→Menunggu Approval→Aktif→Arsip otomatis; Dibatalkan pre-Aktif + escape Aktif→Dibatalkan saat count=0/GAP-4); 4 status_penempatan (Bekerja→Selesai Kontrak/Mengundurkan Diri/Dikeluarkan); atribut dari PRD §5.4; batch normal & Force-Majeur ATOMIK (rollback total); set status_wawancara→Terkirim + markInUse saat batch; markAvailable saat terminal + cek arsip setelah batch; audit enum kanonik (PC_CREATED/SUBMITTED/APPROVED/REJECTED/CANCELLED, BATCH_SENT, FORCE_MAJEUR_ADDED, PLACEMENT_STATUS_CHANGED, RESIGN_*, PLACEMENT_EXPEL_*, CONTAINER_ARCHIVED); konkurensi optimistic `version`→409 + pessimistic FOR UPDATE khusus bulk pull (PRD §7.10); perusahaan_id immutable.
- Alasan: Selaras penuh PRD v0.3.5 + GLOSSARY + ROLES + STATUS_STATE_MACHINE + BUSINESS_RULES + MODULE_LOOKUP_DATA + MODULE_AUTH + MODULE_CANDIDATES + MODULE_JOBS + ARCHITECTURE (D2/D4/D5/D8/D9). Auto-archive trigger terbukti BUKAN GAP (PRD §6.4 Sub-flow 6 + Lampiran B.2: kandidat Bekerja terakhir → terminal, dicek setelah batch; bukan target/tanggal). Tech terverifikasi live 2026-06-29 (Laravel 13.x, PostgreSQL 18 FOR UPDATE READ COMMITTED, spatie/laravel-model-states 2.14.1, queue driver database). Disetujui user 2026-06-29.
- Affects: K2-MODULE_LOOKUP_DATA (perlu master lookup baru `kategori_force_majeur`, CRUD Super Admin + step-up per D-L6), K2-MODULE_CANDIDATES (penamaan `source_participation_id` vs `ref_riwayat_partisipasi` — entitas berbeda, perlu dicatat di skema), K2-MODULE_JOBS, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST
- SUPERSEDES: — (tidak menimpa keputusan final; keputusan #1 menyelaraskan brief misi ke dependency final yang sudah ada)
- Perlu update PRD? Tidak untuk kebenaran inti. Koreksi rujukan §PRD pada brief misi (bukan perubahan isi): atribut Kontainer Penempatan & placement_participants = §5.4 (bukan "§7 Tabel 8"); Force-Majeur = §6.4 Sub-flow 2b (bukan "§9.3"); konkurensi = §7.10 (bukan "§9"). GAP terbuka tindak lanjut: (a) lookup `kategori_force_majeur` belum ada di MODULE_LOOKUP_DATA (FINAL) — perlu adendum kecil/koordinasi pemilik modul; (b) penamaan kolom lintas-modul dicatat untuk DATABASE_SCHEMA. GAP-P1 (step-up brief vs dependency) diselesaikan dengan mengikuti dependency; beralih ke versi brief perlu SUPERSEDES + update PRD.
---
### 2026-06-29 — Tindak lanjut MODULE_[PLACEMENT.md](../modules/MODULE_PLACEMENT.md) — Lookup Force-Majeur + penegasan step-up + catatan schema
- Keputusan: Tiga tindak lanjut lintas-modul dari finalisasi MODULE_PLACEMENT dieksekusi. **(1 — SELESAI)** Lookup baru `kategori_force_majeur` ditambahkan ke MODULE_LOOKUP_[DATA.md](../modules/MODULE_LOOKUP_DATA.md) (FINAL): baris #25 di §4 inventaris + sampel seed Lampiran A.14 (SAKIT_BERAT/MENINGGAL/MASALAH_KELUARGA/BENCANA_ALAM/MASALAH_HUKUM_IMIGRASI/LAINNYA) + catatan GAP-L5; CRUD Super Admin + 🔒 step-up (ikut D-L6/ROLES §5.5), audit `LOOKUP_*` generik. **(3 — SELESAI)** MODULE_[AUTH.md](../modules/MODULE_AUTH.md) §4.5 dipertegas: **Cabut Penempatan** (`PLACEMENT_EXPEL`, status `Dikeluarkan`) eksplisit berada di bawah trigger step-up #3 "pengeluaran kandidat"; daftar TANPA step-up diperluas eksplisit (Approve/Reject/Cancel partisipasi, tambah/approve force-majeur, mengundurkan diri); **GAP-P1 ditutup** di §12 MODULE_AUTH. **(2 — DITUNDA + dicatat)** Beda penamaan `source_participation_id` (Penempatan, PRD §5.4) vs `ref_riwayat_partisipasi` (brief Candidates) = dua hal berbeda; DATABASE_SCHEMA belum dikerjakan, jadi rekonsiliasi/penegasan penamaan kolom WAJIB diputuskan saat DATABASE_SCHEMA dibuat (catatan ini = penanda untuk Database Architect).
- Alasan: Menutup GAP tindak lanjut MODULE_PLACEMENT tanpa menunggu Kelompok 3; #1 & #3 menyentuh dokumen FINAL (LOOKUP_DATA, AUTH) yang sebidang (Kelompok 2) sehingga aman diselaraskan langsung. #2 menyangkut DATABASE_SCHEMA (belum mulai) → cukup dicatat sesuai arahan user. Disetujui user 2026-06-29.
- Affects: K2-MODULE_LOOKUP_DATA, K2-MODULE_AUTH, K2-MODULE_PLACEMENT, K3-DATABASE_SCHEMA
- SUPERSEDES: — (melengkapi entri MODULE_PLACEMENT 2026-06-29; tidak menimpa keputusan final)
- Perlu update PRD? Tidak. Catatan: `kategori_force_majeur` belum ada di PRD → kandidat addendum enum bila ingin dikunci di PRD (lihat GAP-L5 di MODULE_LOOKUP_DATA); rekonsiliasi penamaan kolom lintas-modul menunggu DATABASE_SCHEMA.
---
### 2026-06-29 — MODULE_GUEST_[ACCESS.md](../modules/MODULE_GUEST_ACCESS.md) — Permukaan Tamu bertoken read-only (GA-1..GA-5) final
- Keputusan: MODULE_GUEST_[ACCESS.md](../modules/MODULE_GUEST_ACCESS.md) difinalkan (draft final-ready, disetujui user 2026-06-29). Lima keputusan GATE diputuskan user, SEMUANYA mengikuti rekomendasi & selaras dependency final: **(GA-1)** TTL signed URL R2 untuk aset Tamu = **pendek + di-refresh tiap reload**: dokumen *shareable* **5 menit**, foto thumbnail & video **15 menit** (selaras MODULE_CANDIDATES #4); TTL **TIDAK** disamakan dengan masa berlaku link karena batas hard R2 presigned **max 7 hari/604.800 dtk**. **(GA-2)** URL Video Jikoshokai/Keahlian **default OFF**, hanya tampil bila diaktifkan per link (asas DEFAULT=HIDE). **(GA-3)** Rate-limit endpoint token **dua lapis** (disempurnakan 2026-06-29 atas rekomendasi): **Lapis 1** percobaan token tidak valid **10/menit/IP** (anti-enumerasi), **Lapis 2** buka link sah **60/menit per token** (aman untuk kantor ber-NAT, banyak orang 1 IP publik), + **kode tambahan 5 gagal → lockout 15 menit** (mirror MODULE_AUTH A-4, RateLimiter driver database). Token tidak pernah ditulis ke log mentah (hanya `token_id`). **(GA-4)** Kode tambahan (via WA) = **opsional per link** (ikut PRD §4.3/§5.3). **(GA-5/GAP PRD)** Tamu tetap **read-only murni tanpa kanal tulis** (tidak ada minat/feedback dari perusahaan Jepang lewat surface Tamu) — dikonfirmasi final user. Modul juga mengunci: read-model `GuestCandidateView` = **whitelist persis PRD Lampiran C** (info kontainer nama_ja/tanggal/jenis wawancara; per kandidat Nama Alphabet+Katakana, foto thumbnail signed URL, Umur 歳, Jenis Kelamin 男/女, Level Bahasa Jepang & Inggris, Kualifikasi SSW & Mengemudi, Bidang Diminati, Ringkasan Pendidikan TANPA nama lembaga, Ringkasan Pekerjaan TANPA nama perusahaan, video opsional, dokumen ber-flag `is_shareable` SAJA); **HIDE** enumeratif (catatan internal, status partisipasi & riwayat, data/kontak keluarga, email, telepon, Line ID, alamat lengkap, dokumen identitas Zairyu/paspor/visa, IQ/MTK & psikotes, Nomor Induk); token = **string acak tak tertebak ≠ ID kontainer**; urutan validasi PRD §6.3 Sub-flow 3 (token ada→belum kadaluarsa→kontainer Aktif→kode tambahan bila ada) dengan halaman tolak generik + constant-time compare anti-enumerasi; tanpa listing/browse lintas-kontainer; pagination server-side default 25 (§8.4), sort/filter hanya kolom whitelist; signed URL hanya setelah sesi token valid, tidak di-cache; security headers route Tamu (HSTS, CSP ketat \[nonce vs hash TERGANTUNG stack FE\], X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy no-referrer); halaman Tamu `Cache-Control: no-store` (tanpa CDN/cache publik); audit **`GUEST_ACCESS`** (Lampiran A, `actor_id`=null, detail `{token_id, ip, container_id}`, ke `guest_access_log` §5.3); siklus link `GUEST_LINK_REQUESTED/APPROVED/REJECTED` tetap milik MODULE_JOBS (tidak diduplikasi); UI Tamu berbahasa Jepang (§4.1/§9.4); Tamu read-only tanpa upload (turunan MODULE_CANDIDATES #1, ROLES D-R7/§10).
- Alasan: Selaras penuh PRD v0.3.5 (§4.3, §5.3, §6.3 Sub-flow 3, §7.7, §9.8, Lampiran A/B.6/C) + GLOSSARY (Tamu aktor token) + ROLES (D-R7/§10) + BUSINESS_RULES (whitelist/PII) + MODULE_JOBS (siklus guest_link) + MODULE_CANDIDATES (read-model + no-upload) + MODULE_AUTH (pola lockout A-4) + ARCHITECTURE (D5 cache database, D7 signed URL). Minimisasi data + hardening token surface. Tech terverifikasi live 2026-06-29 (Laravel 13.x/PHP 8.4, flysystem-aws-s3-v3 \^3.x + aws-sdk-php ≥3.337 presigned R2 max 7 hari & checksum WHEN_REQUIRED, RateLimiter/throttle bawaan L13, spatie/laravel-csp v3). Disetujui user 2026-06-29.
- Affects: K1-ROLES_AND_PERMISSIONS, K2-MODULE_JOBS, K2-MODULE_CANDIDATES, K2-MODULE_LOOKUP_DATA, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DEPLOYMENT, K4-DATA_RETENTION_AND_PRIVACY
- SUPERSEDES: — (tidak menimpa keputusan final; GA-1/GA-3 menurunkan keputusan MODULE_CANDIDATES #4 & MODULE_AUTH A-4, GA-5 mengonfirmasi read-only murni yang sudah final)
- Perlu update PRD? Tidak untuk kebenaran inti. GAP/infra terbuka untuk modul lain: (a) **custom domain R2 / proxy** untuk presigned URL agar tidak membocorkan account-id+bucket → keputusan **DEPLOYMENT/ARCHITECTURE** (jangan asumsikan); (b) **CDN aset statis netral** → keputusan **DEPLOYMENT** (PRD §9.6 tidak menyebut CDN); (c) **CSP nonce vs hash** difinalkan saat **stack FE** dikunci (UI_WIREFRAME_NOTES, FE masih `[BELUM DIPUTUSKAN]`); (d) **izin/consent kandidat** untuk berbagi foto+atribut ke perusahaan Jepang (APPI/UU PDP) → **DATA_RETENTION_AND_PRIVACY**; (e) **masa simpan ****`guest_access_log.ip`** (data pribadi) → **DATA_RETENTION_AND_PRIVACY**; (f) **event audit percobaan akses gagal** belum ada di enum Lampiran A → log keamanan non-audit (default) atau usulan kecil PRD. GAP PRD kanal feedback perusahaan Jepang **DITUTUP** = read-only murni (GA-5).
---
### 2026-06-30 — DATABASE_[SCHEMA.md](../technical/DATABASE_SCHEMA.md) — Skema fisik PostgreSQL terkonsolidasi (final)
- Keputusan: DATABASE_[SCHEMA.md](../technical/DATABASE_SCHEMA.md) difinalkan (disetujui user 2026-06-30). Lima keputusan GATE + empat resolusi GAP-DB diputuskan user, semua mengikuti rekomendasi: **(GATE-1) PK = ****`bigint GENERATED ALWAYS AS IDENTITY`** untuk semua tabel domain (hemat RAM/index di VPS 2vCPU/2GB; UUIDv7 16B vs 8B menggandakan tiap index FK) — pengecualian framework: `sessions` (string id), `cache`/`cache_locks` (string key), `notifications` (uuid), `jobs`/`failed_jobs` (bigserial bawaan Laravel). **(GATE-2) Tabel anak kandidat = normalisasi penuh** (tabel terpisah per entitas, bukan jsonb) demi kontrol PII granular & whitelist Tamu; kualifikasi direalisasikan sebagai 5 tabel bertipe (`candidate_qual_english/japanese/ssw/driving/other`) yang mewujudkan konsep `candidate_qualification` MODULE_CANDIDATES. **(GATE-3) ****`audit_log`**** = satu tabel + index** (volume kecil 500–3.000 kandidat/±15 user; partisi per-bulan didokumentasikan sebagai opsi masa depan, belum diterapkan). **(GATE-4) Normalisasi nama pg_trgm "apa adanya"** — TANPA citext/unaccent (cek-kemiripan pakai `similarity()>=0.4` eksplisit pada `lower(nama_alphabet)` & `nama_katakana`, selaras BR-DUP/MODULE_CANDIDATES #6). **(GATE-5) NIK = tabel ****`nik_counter(year PK, last_value)`**** + UPSERT transaksional** (`INSERT...ON CONFLICT(year) DO UPDATE`), year = `EXTRACT(YEAR FROM now() AT TIME ZONE 'Asia/Tokyo')`, format `K-YYYY-NNNNN`, gap diizinkan, unique permanen di `kandidat.nomor_induk` (BR-NIK). Resolusi GAP: **(GAP-DB1)** rekonsiliasi partisipasi — dikunci dua tabel konkret `participation` (Wawancara) + `placement_participants` (Penempatan); tabel generik `candidate_participation`/`container_type` di MODULE_CANDIDATES §13 **DI-SUPERSEDE** (riwayat partisipasi kandidat = view/service union lintas dua tabel, BUKAN tabel fisik ketiga). **(GAP-DB2)** field `candidate_immigration` (no. paspor, masa berlaku, no. zairyu, alamat zairyu, jenis visa) diturunkan dari daftar HIDE Tamu + lookup `jenis_visa`/`jenis_dokumen_identitas` (PRD §5.2 tak merinci → GAP MODUL, disetujui user). **(GAP-DB3)** nomor paspor/zairyu = kolom PII biasa (TANPA enkripsi kolom-level; envelope encryption hanya untuk FILE dokumen identitas per MODULE_CANDIDATES #3), terlindungi via pola anonimisasi PII. **(GAP-DB4)** `audit_log.action_type` = PHP backed-enum (validasi aplikasi), BUKAN CHECK keras DB, agar penambahan event non-breaking. **Penguncian kanonik:** kolom referensi partisipasi asal di `placement_participants` = `source_participation_id` (nullable FK→`participation`), NULL ⇒ jalur Force-Majeur, dengan CHECK `(source_participation_id IS NULL) = (kategori_force_majeur_id IS NOT NULL AND alasan_force_majeur IS NOT NULL)` — menutup catatan rekonsiliasi penamaan dari tindak lanjut MODULE_PLACEMENT (`ref_riwayat_partisipasi` dipetakan, BUKAN nama baru). Skema juga mengunci: konvensi snake_case (tabel plural, kolom singular), timestamptz simpan UTC/render JST, optimistic `version` di semua agregat mutable + pessimistic FOR UPDATE bulk pull (maks 50/operasi), 7 mesin status via backed enum + CHECK, metadata envelope encryption (`object_key/dek_wrapped/dek_iv/content_iv/auth_tag/kek_version/algo`), GIN gin_trgm_ops, composite index pagination default 25 + whitelist sort/filter (kolom HIDE Tamu dikecualikan), `audit_log` immutable Super Admin read-only, soft-delete `deleted_at` vs tombstone anonimisasi PII permanen, tanpa FK lintas-modul (referensi via service), satu ekstensi PG = `pg_trgm` saja.
- Alasan: Konsolidasi seluruh kebutuhan persistensi K1+K2 (FINAL) menjadi satu skema fisik ternormalisasi sehat, ber-index tepat, aman PII, dan muat di VPS 2vCPU/2GB. Selaras penuh PRD v0.3.5 + seluruh dependency final. Tech terverifikasi live 2026-06-30 (PostgreSQL 18.x GA, pg_trgm/GIN gin_trgm_ops, Laravel 13 migration jsonb/CHECK/partial/GIN index; UUIDv7 ditolak demi efisiensi index di 2GB; citext/unaccent tak dipakai karena cek-kemiripan "apa adanya"). Disetujui user 2026-06-30.
- Affects: K2-MODULE_CANDIDATES (supersede tabel generik `candidate_participation` → realisasi `participation`+`placement_participants`; kualifikasi jadi 5 tabel), K2-MODULE_PLACEMENT (penguncian `source_participation_id`), K2-MODULE_JOBS, K2-MODULE_LOOKUP_DATA, K2-MODULE_AUTH, K2-MODULE_GUEST_ACCESS, K1-STATUS_STATE_MACHINE, K3-UI_WIREFRAME_NOTES, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY, K4-DEPLOYMENT, K4-BACKUP_AND_RECOVERY
- SUPERSEDES: Konsep tabel generik `candidate_participation` (container_type WAWANCARA/PENEMPATAN) di MODULE_CANDIDATES §13 — ditandai USANG pada level skema fisik (disetujui user 2026-06-30); rekonsiliasi ini memang didelegasikan ke DATABASE_SCHEMA oleh entri "Tindak lanjut MODULE_PLACEMENT" 2026-06-29 (butir #2). Tidak menimpa entri final DECISIONS_LOG manapun. Pemilik MODULE_CANDIDATES disarankan menambahkan catatan rujukan ke entri ini.
- Perlu update PRD? Tidak untuk kebenaran inti (PRD §5.4 sudah memakai `source_participation_id`; skema mengonsolidasi tanpa mengubah aturan). GAP terbuka tersisa: GAP-DB2 (field imigrasi rinci) & seed beberapa lookup menunggu konfirmasi kumiai; rincian jadwal retensi PII menunggu DPO; opsi partisi `audit_log` ditinjau bila volume tumbuh.
---
### 2026-06-30 — DATABASE_[SCHEMA.md](../technical/DATABASE_SCHEMA.md) + MODULE_[CANDIDATES.md](../modules/MODULE_CANDIDATES.md) — Penghapusan `candidate_identity_doc` + sinkronisasi field Kandidat ke PRD §5.2 + nama perusahaan riwayat kerja tampil ke Tamu
- Keputusan: Atas instruksi user (setelah diskusi dgn atasan) dilakukan tiga kelompok perubahan pada skema kandidat, semua mengikuti PRD §5.2 sebagai sumber kebenaran. **(1) Penghapusan ****`candidate_identity_doc`****:** tabel multi-dokumen `candidate_identity_doc` (Zairyu+Paspor) **DIHAPUS** dari DATABASE_SCHEMA karena tidak sesuai PRD §5.2 yang hanya menyebut **satu "Foto Zairyu Card: URL link"** (Paspor = hanya nomor, tanpa scan). Foto Zairyu kini disimpan sebagai **kolom ber-prefix ****`zairyu_*`**** (envelope encryption: object_key/dek_wrapped/dek_iv/dek_auth_tag/content_iv/content_auth_tag/algo/kek_version/mime_type/size_bytes/uploaded_by/uploaded_at)** pada `candidate_immigration` (1:1). MODULE_CANDIDATES diselaraskan (§4.3 entitas, §6.2 envelope encryption, §9 tabel utama, §15 invariant #3/#4). Envelope encryption (keputusan MODULE_CANDIDATES #3) tetap berlaku + audit `IDENTITY_DOC_VIEWED`. Lookup `jenis_dokumen_identitas` kini **tidak terpakai** — sementara dipertahankan di daftar 25 lookup (perlu konfirmasi apakah dihapus). **(2) Nama perusahaan riwayat kerja → DITAMPILKAN ke Tamu:** `candidate_work.nama_perusahaan` kini ditampilkan ke Tamu (sebelumnya HIDE) + field opsional baru `perusahaan_penanggung` (nama TSK/Kumiai penanggung saat pengalaman kerja di Jepang, PRD §5.2 — sebelumnya terlewat). **(3) Sinkronisasi field Kandidat ke PRD §5.2 (rapikan gap):** ditambahkan `candidate_physical.lingkar_perut_cm`; `candidate_qual_*.tanggal_akuisisi` + `url_file`; `candidate_self_promo.video_keahlian_url` + `final_laporan_psikotes` (rename `video_url`→`video_jikoshokai_url`); `candidate_family.tanggal_lahir` (umur computed). Tipe tanggal diselaraskan ke DATE: `candidate_work` & `candidate_education` (`tanggal_masuk`/`tanggal_keluar`, sebelumnya kolom tahun `SMALLINT`). Kolom hasil invensi (tidak ada di PRD §5.2) **dihapus** demi prinsip anti-invent: `candidate_work.posisi`/`deskripsi`, `candidate_self_promo.motivasi`, `candidate_family.pekerjaan`/`keterangan`, `candidate_qual_*.nomor_sertifikat`. Dicatat sebagai GAP-DB5/DB6/DB7 di DATABASE_SCHEMA §12.
- Alasan: PRD adalah sumber kebenaran tertinggi ("jika terjadi konflik, PRD yang berlaku"). Audit ketelitian user menemukan (a) field `candidate_identity_doc` melebihi cakupan PRD §5.2, (b) field `perusahaan_penanggung` PRD §5.2 terlewat, (c) sejumlah field PRD §5.2 lain belum lengkap & beberapa kolom hasil invensi belum sesuai. Disetujui & diinstruksikan user 2026-06-30.
- Affects: K2-MODULE_CANDIDATES (penghapusan candidate_identity_doc + Foto Zairyu pindah ke candidate_immigration; sinkron field), K2-MODULE_LOOKUP_DATA (lookup `jenis_dokumen_identitas` jadi tak terpakai — perlu konfirmasi), K2-MODULE_GUEST_ACCESS (nama perusahaan riwayat kerja kini masuk whitelist Tamu — beda dari Lampiran C), K4-DATA_RETENTION_AND_PRIVACY (Foto Zairyu envelope di candidate_immigration), K4-API_CONTRACTS, K4-SECURITY_CHECKLIST.
- SUPERSEDES: Memperbarui **GAP-DB2** entri DATABASE_SCHEMA 2026-06-30 (struktur `candidate_immigration` kini mencakup kolom Foto Zairyu `zairyu_*`); menghapus penggunaan `candidate_identity_doc` yang dirujuk di entri MODULE_CANDIDATES #3 (2026-06-29) — mekanisme envelope encryption tetap, hanya lokasi kolom berpindah. Tidak menimpa keputusan final lain.
- Perlu update PRD? **Ya (perlu update):** (1) **PRD Lampiran C ****`GuestCandidateView`**** & §7.7** — "Ringkasan Riwayat Pekerjaan" yang semula HIDE nama perusahaan harus diubah agar **menampilkan nama perusahaan** ke Tamu (sesuai keputusan user ini); ini satu-satunya perubahan yang mengubah kebenaran inti PRD dan **menunggu penerapan ke PRD + kenaikan versi**. (2) Penghapusan `candidate_identity_doc` & sinkronisasi field lain **menyelaraskan** skema ke PRD §5.2 (tidak mengubah PRD). Catatan: konfirmasi nasib lookup `jenis_dokumen_identitas` (dipertahankan/dihapus) masih terbuka.
---
### 2026-06-30 — PRD_Kakehashi (Lampiran C + Riwayat Revisi → v0.3.6) + MODULE_LOOKUP_DATA + DATABASE_SCHEMA — Update PRD visibilitas nama perusahaan ke Tamu + penghapusan lookup jenis_dokumen_identitas
- Keputusan: (1) **PRD diupdate ke v0.3.6** — `GuestCandidateView` (Lampiran C) ringkasan Riwayat Pekerjaan menjadi *Nama Perusahaan + Bidang Pekerjaan + durasi*; nama perusahaan kini **DITAMPILKAN ke Tamu** (sebelumnya disembunyikan). Ditambahkan baris Riwayat Revisi 0.3.6 di §1. Menutup GAP-DB5. (2) Lookup `jenis_dokumen_identitas` **DIHAPUS**: di MODULE_LOOKUP_DATA (inventaris 25→24 — row §4, seed Lampiran A.11, GAP-L6) dan di DATABASE_SCHEMA (§4 Master & lookup 29→28, §5.5 daftar lookup, GAP-DB2/GAP-DB7, §13 HANDOFF), mengikuti penghapusan tabel `candidate_identity_doc`. Dokumen identitas PRD §5.2 hanya Foto Zairyu Card (kolom envelope `zairyu_*` pada `candidate_immigration`).
- Alasan: (1) keputusan user (setelah diskusi dengan atasan) menampilkan nama perusahaan riwayat kerja ke Tamu — perubahan kebenaran inti whitelist Tamu sehingga wajib direfleksikan di PRD (sumber kebenaran). (2) lookup `jenis_dokumen_identitas` menjadi orphan setelah penghapusan struktur multi-dokumen identitas; user memutuskan ikut dihapus.
- Affects: K1-PRD (Lampiran C, Riwayat Revisi, versi 0.3.6), K2-MODULE_LOOKUP_DATA (inventaris 24 lookup), K3-DATABASE_SCHEMA (Master & lookup 28, GAP-DB5 RESOLVED di sisi PRD, GAP-DB7 updated), K2-MODULE_GUEST_ACCESS (whitelist Tamu kini memuat nama perusahaan riwayat kerja — perlu sinkron pada pass berikutnya), K4-API_CONTRACTS (read-model GuestCandidateView).
- SUPERSEDES: GAP-DB5 (DATABASE_SCHEMA) kini terselesaikan di sisi PRD; melanjutkan keputusan penghapusan candidate_identity_doc (entri sebelumnya).
- Perlu update PRD? Sudah dilakukan (v0.3.6 di isi dokumen). Catatan terbuka: judul halaman PRD masih "v0_3_5" — opsi rename ke v0_3_6 menunggu konfirmasi user; detail whitelist di MODULE_GUEST_ACCESS perlu disinkronkan menyusul.
---
### 2026-06-30 — PRD_Kakehashi (v0.3.6 → v0.3.7) + DATABASE_SCHEMA — Klarifikasi penyimpanan Foto Zairyu Card (Opsi A: upload terenkripsi)
- Keputusan: Temuan audit user atas DATABASE_SCHEMA (`candidate_immigration`) menyoroti konflik semu antara redaksi PRD §5.2 "Foto Zairyu Card: URL link" vs kolom fisik `zairyu_*` (envelope encryption). Diputuskan user **Opsi A** — pertahankan model **upload file terenkripsi**. (1) **PRD dinaikkan ke v0.3.7** (judul halaman → PRD_Kakehashi_v0_3_7): §5.2 field Foto Zairyu Card diperjelas dari "URL link" menjadi *unggah file (gambar/PDF), dienkripsi app-level (envelope encryption) sebelum upload (§9.1/§9.8), diakses via signed URL pendek 5–15 mnt — bukan URL eksternal yang diketik staf*; ditambah baris Riwayat Revisi 0.3.7 (§1) + footer versi disinkronkan. (2) **DATABASE_SCHEMA**: komentar `candidate_immigration` dirapikan (tidak lagi mengutip "URL link" sebagai kebenaran), bagian Foto Zairyu pada GAP-DB7 & GAP-DB2 ditandai **RESOLVED**. Kolom `zairyu_*` (envelope) TIDAK berubah — terbukti implementasi yang benar & konsisten; widget upload di mockup UI sesuai skema.
- Alasan: PRD dibaca utuh sejak awal sudah mewajibkan upload+enkripsi (§9.1 "envelope encryption sebelum upload"; §9.8 signed URL pendek untuk akses file). Frasa "URL link" di §5.2 = cara akses (signed URL atas file ter-upload), bukan instruksi staf mengetik URL. Interpretasi literal "URL ketik" justru melanggar §9.1 dan melemahkan keamanan dokumen identitas. Maka ini klarifikasi redaksi (non-breaking), bukan perubahan kebenaran inti. Disetujui user 2026-06-30 (Opsi A).
- Affects: K3-DATABASE_SCHEMA (komentar candidate_immigration, GAP-DB2/DB7 RESOLVED), K2-MODULE_CANDIDATES (envelope encryption #3 — penjelasan/lokasi kolom, tanpa perubahan mekanisme), K3-UI_WIREFRAME_NOTES (widget upload Foto Zairyu), K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY.
- SUPERSEDES: Memperjelas redaksi "Foto Zairyu Card: URL link" pada entri PRD v0.3.6 (2026-06-30) — frasa tersebut kini ditegaskan bermakna signed URL akses atas file ter-upload. Tidak menimpa keputusan final lain.
- Perlu update PRD? Sudah dilakukan (v0.3.7 — §5.2, Riwayat Revisi, footer, judul halaman). Tidak ada item PRD terbuka tersisa dari temuan ini.
---
### 2026-06-30 — PRD_Kakehashi (v0.3.7 → v0.3.8) + DATABASE_SCHEMA + MODULE_LOOKUP_DATA — Restrukturisasi field geografis: Tempat Lahir (dropdown Kota) vs Alamat terstruktur (Opsi B)
- Keputusan: Atas temuan & keputusan user (Opsi B, 2026-06-30) field geografis Data Card Kandidat direstrukturisasi — **mengubah kebenaran inti**. **(1) Tempat Lahir = dropdown Kota/Kabupaten saja** (sebelumnya hierarki Kecamatan/Kota/Provinsi). **(2) Provinsi/Kota-Kabupaten/Kecamatan dipindahkan menjadi komponen Alamat terstruktur**; **Alamat Lengkap** berubah dari *teks bebas* menjadi **dropdown bertingkat Provinsi → Kota/Kabupaten → Kecamatan + teks bebas untuk detail (jalan/RT/RW)**. Diterapkan: **PRD v0.3.8** (§5.2 Tempat Lahir + Alamat Lengkap, Riwayat Revisi 0.3.8, Versi, judul halaman → PRD_Kakehashi_v0_3_8); **DATABASE_SCHEMA** (`candidate`: hapus `tempat_lahir TEXT` & `alamat TEXT`; tambah `tempat_lahir_kota_id` FK→kota_kabupaten; ganti `provinsi_id`/`kota_kabupaten_id`/`kecamatan_id` → `alamat_provinsi_id`/`alamat_kota_kabupaten_id`/`alamat_kecamatan_id` + `alamat_detail TEXT`; catat GAP-DB8); **MODULE_LOOKUP_DATA** (§4: provinsi=Alamat, kota_kabupaten=Tempat lahir+Alamat, kecamatan=Alamat; revisi Q2).
- Alasan: User mengoreksi bahwa kota/kecamatan/provinsi secara semantik adalah bagian **alamat**, sedangkan **tempat lahir** cukup berupa kota. Memisahkan kedua konsep menghilangkan pemodelan ganda (`tempat_lahir TEXT` bebas + 3 FK geo yang ambigu) dan menyelaraskan ke maksud bisnis. PRD = sumber kebenaran tertinggi; perubahan struktur data = kebenaran inti → versi PRD dinaikkan. Disetujui user 2026-06-30 (Opsi B).
- Affects: K1-PRD (§5.2, Riwayat Revisi, versi 0.3.8), K3-DATABASE_SCHEMA (`candidate`, GAP-DB8), K2-MODULE_LOOKUP_DATA (§4 pemakaian provinsi/kota_kabupaten/kecamatan, Q2), K2-MODULE_CANDIDATES (field Data Card — perlu sinkron pada pass berikutnya bila merinci kolom), K3-UI_WIREFRAME_NOTES (form Tempat Lahir vs Alamat), K4-API_CONTRACTS.
- SUPERSEDES: Bagian **struktur** keputusan **Q2 MODULE_LOOKUP_DATA** (2026-06-29: tempat lahir = hierarki Kecamatan/Kota/Provinsi via `provinsi.negara_code`) — kini tempat lahir = Kota/Kabupaten saja; dukungan multi-negara dipertahankan via `kota_kabupaten → provinsi.negara_code`. Juga memperbarui redaksi PRD §5.2 "Tempat Lahir (Kecamatan/Kota/Provinsi)" pada entri v0.3.7 sebelumnya.
- Perlu update PRD? Sudah dilakukan (v0.3.8 — §5.2, Riwayat Revisi, Versi, judul halaman). Tidak ada item PRD terbuka tersisa dari temuan ini.
---
### 2026-07-01 — PRD_Kakehashi (v0.3.8 → v0.3.9) + DATABASE_SCHEMA + MODULE_LOOKUP_DATA + MODULE_CANDIDATES — Pembalikan "dokumen identitas" → koleksi Dokumen Peserta + arsitektur file Google Drive privat
- Keputusan: Atas koreksi user (arahan atasan sebelumnya keliru), "dokumen identitas" **bukan** hanya Foto Zairyu Card melainkan **koleksi Dokumen Peserta** (KTP, KK, Ijazah, Kartu Zairyu, Paspor, SKCK, dll). **(1)** Ditambahkan tabel berulang **`candidate_document`** (DATABASE_SCHEMA §5.1: `candidate_id` CASCADE, `jenis_dokumen_id`→lookup baru `jenis_dokumen`, `url_dokumen` = link **Google Drive privat**, `nama_file`, `catatan`, `uploaded_by`, `sort_order`), pola seperti `candidate_work`/`candidate_education`; Domain Kandidat 15→16 tabel. **(2)** Lookup **`jenis_dokumen`** di-add kembali (MODULE_LOOKUP_DATA §4 #25 + seed Lampiran A.11: KTP/KK/IJAZAH/PASPOR/ZAIRYU_CARD/SKCK/LAINNYA; inventaris 24→25) — membalik penghapusan `jenis_dokumen_identitas` 2026-06-30. **(3)** **Foto Zairyu Card** pindah dari kolom `zairyu_*` (envelope) di `candidate_immigration` menjadi satu baris `candidate_document` (jenis `ZAIRYU_CARD`) — **seluruh kolom ****`zairyu_*`**** envelope DIHAPUS**. **(4) Arsitektur file baru:** semua referensi file = **link Google Drive privat** ("tidak diset public", URL input), KECUALI **foto wajah** (`candidate_photo`, tetap upload R2+SSE + signed URL) dan **video** (`candidate_self_promo.video_*_url` = embed URL). **Subsistem envelope encryption + R2 signed URL + aws-sdk/Flysystem untuk dokumen DIHAPUS** (R2 hanya untuk foto wajah). **(5) Akses Tamu:** sertifikat shareable via Google Drive **"anyone with link"** (opsi paling sederhana, keamanan tidak terlalu ketat, keputusan user). PRD dinaikkan ke **v0.3.9** (§5.2 tambah Field Dokumen Peserta + Foto Zairyu jadi dokumen + video embed URL; §9.1/§9.6/§9.8 model file storage; Riwayat Revisi, Versi, judul halaman, footer). MODULE_CANDIDATES diselaraskan (§1 scope, §2 keputusan #3/#4 DISUPERSEDE, §4.3, §6 rewrite, §9/§9.1, §15/§16/§17).
- Alasan: Koreksi arahan atasan — "kumpulan dokumen identitas" ternyata koleksi dokumen umum peserta, & dokumen disimpan di Google Drive (tidak public). Membalik keputusan 2026-06-30 (penghapusan candidate_identity_doc/jenis_dokumen_identitas) & Opsi A (envelope, v0.3.7). PRD = sumber kebenaran; perubahan struktur = kebenaran inti → versi PRD naik. Disetujui user 2026-07-01.
- Affects: K1-PRD (§5.2, §9.1/§9.6/§9.8, Lampiran C, Riwayat Revisi, v0.3.9), K3-DATABASE_SCHEMA (candidate_document, drop zairyu_\*, GAP-DB9), K2-MODULE_LOOKUP_DATA (lookup jenis_dokumen), K2-MODULE_CANDIDATES (file handling), K2-MODULE_GUEST_ACCESS (akses file Drive "anyone with link"), K1-ARCHITECTURE (D7 file storage), K4-DATA_RETENTION_AND_PRIVACY, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST.
- SUPERSEDES: Entri 2026-06-30 (penghapusan `candidate_identity_doc` + penghapusan lookup `jenis_dokumen_identitas`) & entri 2026-06-30 (Opsi A Foto Zairyu upload envelope, v0.3.7); GAP-DB7 DATABASE_SCHEMA (di-supersede oleh GAP-DB9); MODULE_CANDIDATES keputusan #3 (envelope encryption) & #4 (signed URL TTL bertingkat) ditandai DISUPERSEDE.
- Perlu update PRD? Sudah dilakukan (v0.3.9). Catatan: integrasi Google Drive (service account/permission) & kebijakan hapus/anonimisasi file Drive → DATA_RETENTION_AND_PRIVACY/DEPLOYMENT; ARCHITECTURE D7 & MODULE_GUEST_ACCESS perlu disinkronkan menyusul.
---
### 2026-07-01 — `DESIGN.md` / STATUS_STATE_MACHINE / UI_WIREFRAME_NOTES — Warna badge ketersediaan: Tersedia vs Sedang Dipakai wajib dibedakan
- Keputusan: Status ketersediaan kandidat ditampilkan dengan **warna berbeda yang jelas**: **Tersedia = success (hijau, #DCFCE7/#166534)**, **Sedang Dipakai = neutral/zinc (abu, #F4F4F5/#3F3F46)** — BUKAN teal/info/hijau. `DESIGN.md` §3 diperbarui (Sedang Dipakai: semula info/biru → neutral/zinc, keputusan user 2026-07-01). Ditambahkan **catatan presentasi lintas-file** di STATUS_STATE_MACHINE §0 (menegaskan keduanya wajib beda warna, merujuk `DESIGN.md`), dan **mockup K1 (Kandidat List) dikoreksi ke neutral/zinc**.
- Alasan: Pada mockup K1 awal, Sedang Dipakai sempat memakai teal yang terlalu mirip hijau Tersedia sehingga sulit dibedakan sekilas (temuan user). User memilih **neutral/zinc** untuk Sedang Dipakai agar kontras maksimal dengan hijau Tersedia dan tidak berkesan aksi/informasi (biru); `DESIGN.md` §3 diperbarui + didokumentasikan lintas-file agar konsisten.
- Affects: K3-UI_WIREFRAME_NOTES (mockup K1 & layar berkolom ketersediaan), K1-STATUS_STATE_MACHINE (catatan presentasi §0), [K3-DESIGN.md](http://K3-DESIGN.md) (§3: Sedang Dipakai info→neutral/zinc).
- SUPERSEDES: — (menegaskan peta warna `DESIGN.md` §3 yang sudah ada; mengoreksi deviasi teal pada mockup K1).
- Perlu update PRD? Tidak (warna = presentation layer, bukan kebenaran inti PRD).
---
### 2026-07-01 — DATABASE_SCHEMA / MODULE_JOBS / MODULE_PLACEMENT / UI_WIREFRAME_NOTES / PRD — Kode Kontainer human-readable (W-/P-)
- Keputusan: Ditambahkan **identifier human-readable** pada kedua kontainer karena PK `bigint` internal tidak diekspos ke UI (§1.2), sementara mockup W1 sempat menampilkan penomoran W-2026-… tanpa kolom identifier di skema. **`interview_container.kode_kontainer`**** = ****`W-YYYY-NNNNN`** & **`placement_container.kode_kontainer`**** = ****`P-YYYY-NNNNN`** (prefix satu huruf, konsisten dgn NIK `K-YYYY-NNNNN`). Tipe `VARCHAR(13)`, `UNIQUE` (nullable → Draft NULL), **immutable** setelah di-assign; di-assign **saat submit pertama** (Draft→Menunggu Approval). Ditambahkan tabel counter baru **`container_counter (prefix, year, last_value, updated_at, PK(prefix, year))`** — UPSERT `ON CONFLICT (prefix, year) DO UPDATE`, `year = EXTRACT(YEAR FROM now() AT TIME ZONE 'Asia/Tokyo')` (reset per-tahun JST, gap diizinkan), pola meniru `nik_counter`. **Wajib MVP**, tanpa backfill (belum ada data). Tamu = kode disembunyikan; kode kontainer yang dibatalkan tidak didaur ulang.
- Alasan: Menutup gap identifier (temuan user) — PK internal tak layak jadi nomor referensi bisnis; prefix satu huruf W/P selaras gaya NIK. Default (prefix W-/P-, wajib MVP, reset tahunan JST, immutable) diputuskan user (rekomendasi agent diterima) 2026-07-01.
- Affects: K3-DATABASE_SCHEMA (kolom `kode_kontainer` di interview_container & placement_container + tabel `container_counter`), K2-MODULE_JOBS (§2.1), K2-MODULE_PLACEMENT (§2.1), K3-UI_WIREFRAME_NOTES (kolom Kode di W1/P1), K4-API_CONTRACTS.
- SUPERSEDES: — (menambah field turunan operasional; tidak menimpa keputusan final).
- Perlu update PRD? Ya (adendum ringan, TANPA naik versi — tetap v0.3.9): ditambahkan catatan field "Kode Kontainer" di §5.3 (Kontainer Wawancara) & §5.4 (Data Kontainer Penempatan) sebagai field turunan sistem. Bukan perubahan kebenaran inti (identifier derivatif, bukan aturan bisnis baru).
---
### 2026-07-01 — `DESIGN.md` / UI_WIREFRAME (K1, K2, anchor) — Ikon pada badge status (approval & ketersediaan)
- Keputusan: Badge status memakai **ikon + teks + warna** (bukan dot). Pemetaan ikon kanonik: Menunggu Tinjauan/Approval/Wawancara=jam (clock); Disetujui/Aktif/Lulus/Bekerja=check-circle; Tersedia=check; Ditolak/Dikeluarkan/Dibatalkan/Tidak Lolos=x-circle; Mengundurkan Diri=arrow-uturn-left; Terkirim/Selesai Kontrak/Diterapkan=badge-check (teal); Sedang Dipakai=lock; Draft/Arsip/Kadaluarsa/Ditutup=neutral. Dikodifikasi di `DESIGN.md` §3/§6.2; diterapkan ke K1 (badge tabel + chip filter), anchor (K3) & K2 (header). Warna token tidak berubah.
- Alasan: Permintaan atasan agar status approval & ketersediaan lebih jelas; selaras aturan `DESIGN.md` §1/§6.2 (status tak pernah warna-saja → ikon+teks+warna) & aksesibilitas WCAG AA.
- Affects: K3-UI_WIREFRAME_NOTES (semua layar berbadge status), [K3-DESIGN.md](http://K3-DESIGN.md) (§3/§6.2 peta ikon).
- SUPERSEDES: — (melengkapi peta badge §3; badge sebelumnya pakai dot).
- Perlu update PRD? Tidak (presentation layer).
---
### 2026-07-11 — DATABASE_SCHEMA / PRD (v0.3.9 → v0.3.10) / BUSINESS_RULES — Audit enum & audit_log lengkap + guard manajemen user (turunan verifikasi UI S1–S5)
- Keputusan: Menutup GAP data-model yang muncul saat verifikasi devil's-advocate UI S1–S5. **(1) Enum audit ditambah** (backed-enum aplikasi, non-breaking): **Lookup** `LOOKUP_UPDATED` + `LOOKUP_REACTIVATED` (S1 edit label & reaktivasi; `code` tetap immutable); **Company** `COMPANY_CREATED` + `COMPANY_UPDATED` + `COMPANY_REACTIVATED` (S3 master perusahaan create/edit/soft-disable langsung Super Admin; jalur request tetap `COMPANY_REQUESTED/APPROVED/REJECTED`, `COMPANY_APPROVED` = perusahaan lahir via request); **User/Role** `USER_UPDATED` + `USER_REACTIVATED` + `PASSWORD_RESET_BY_ADMIN` (S4; `PASSWORD_RESET_BY_ADMIN` dibedakan dari `PASSWORD_CHANGED` self-service). **(2) ****`audit_log`**** (§5.7)** ditambah kolom **`actor_role_snapshot TEXT`** (snapshot peran aktor SAAT kejadian — bukan join live ke `users`, karena peran bisa berubah; NULL utk Tamu/sistem) & **`user_agent TEXT`** (forensik viewer S5); struktur final = `id, actor_id, actor_role_snapshot, action_type, entity_type, entity_id, detail(JSONB), ip, user_agent, created_at`. **(3) GAP-DB4 RESOLVED:** `action_type` = **backed-enum PHP (daftar tetap), TANPA CHECK keras DB** — daftar A.1 = kontrak filter S5. **(4) Guard manajemen user (server-side, BUSINESS_RULES §8A BR-USR-01..05):** (a) larangan menonaktifkan/menurunkan Super Admin `Aktif` terakhir (min. 1 wajib; cek dalam transaksi + row lock anti-race) → `USR_LAST_SUPERADMIN` (422); (b) larangan aksi ke diri sendiri (nonaktif → `USR_SELF_DEACTIVATE`, ubah peran → `USR_SELF_ROLE`, reset password sendiri → `USR_SELF_RESET`; 422); reset-by-admin set `must_change_password=TRUE`; reaktivasi → `USER_REACTIVATED`. **(5) Loop Tamu G1–G3 (disiapkan lebih awal):** whitelist `GuestCandidateView` = PRD Lampiran C (final, tidak berubah); token = string acak ≥256-bit (disimpan sebagai `token_hash`, ≠ ID kontainer); kode akses tambahan = **6 digit numerik** (disimpan hash, constant-time compare), lockout 5 gagal/15 mnt + rate-limit token 10/mnt/IP (invalid) & 60/mnt/token (MODULE_GUEST_ACCESS GA-3); masa berlaku token = `guest_link.tanggal_kadaluarsa` wajib saat pembuatan + otomatis mati bila kontainer Ditutup/Dibatalkan. **DITUNDA:** enum `GUEST_ACCESS_DENIED`/`GUEST_LOCKED` TIDAK ditambahkan ke `audit_log` — percobaan akses gagal Tamu tetap ke **log keamanan aplikasi** (bukan audit_log), konsisten MODULE_GUEST_ACCESS §9 (aktor untrusted/null, hindari noise enumerasi); ditinjau ulang di loop G1–G3. `GUEST_ACCESS` (sukses) tetap kanonik — UI memetakan label "GUEST_LINK_ACCESSED" → `GUEST_ACCESS`.
- Alasan: UI S1–S5 sementara memakai enum terdekat + placeholder agar HTML tak meng-invent field; data-model kini dirapikan agar UI dapat diselaraskan. Snapshot peran wajib karena audit harus merefleksikan peran pada saat kejadian, bukan peran terkini. Guard last-Super-Admin & self-action mencegah lockout administratif & eskalasi/penurunan hak tak sengaja. Backed-enum aplikasi menjaga penambahan non-breaking + memberi daftar filter FINAL untuk S5. PRD = sumber kebenaran; penambahan enum/kolom + aturan bisnis = kebenaran inti → versi PRD dinaikkan ke v0.3.10. Diputuskan user 2026-07-11.
- Affects: K3-DATABASE_SCHEMA (§7 enum, §5.7 audit_log, GAP-DB4), K1-PRD (Lampiran A, §4.2, §5.1, §6.1, Riwayat Revisi, v0.3.10), K1-BUSINESS_RULES (§8A BR-USR), K3-UI_WIREFRAME (S1/S3/S4/S5 + filter S5), K2-MODULE_AUTH (PASSWORD_RESET_BY_ADMIN + guard), K2-MODULE_LOOKUP_DATA (LOOKUP_UPDATED/REACTIVATED), K2-MODULE_GUEST_ACCESS (persiapan loop Tamu), K4-API_CONTRACTS, K4-SECURITY_CHECKLIST.
- SUPERSEDES: Menutup GAP-DB4 DATABASE_SCHEMA (sebelumnya "minor / perlu konfirmasi"). Tidak menimpa keputusan final lain.
- Perlu update PRD? Ya — SUDAH DITERAPKAN ke v0.3.10 (Lampiran A enum + skema audit_log, §4.2/§6.1 guard, §5.1 field audit, Riwayat Revisi, footer, judul halaman). Catatan: enum audit akses Tamu gagal (GUEST_ACCESS_DENIED/GUEST_LOCKED) DITUNDA ke loop Tamu G1–G3.
---
### 2026-07-13 — Infra baseline MVP: VPS 4 vCPU / 8 GB + Redis co-located
- Keputusan: Naikkan baseline infra dari **Tencent Lighthouse 2vCPU/2GB** ke **4 vCPU / 8 GB RAM / 100–120 GB SSD** (single VPS, no HA). **Redis diizinkan di VPS yang sama** untuk **cache, session, queue, rate limit**. Queue production = **Redis + 2 worker** (Supervisor). Notifikasi tetap in-app + polling ≤60 dtk (tanpa WebSocket). Unique constraint + transaksi DB **tetap** sumber kebenaran anti-duplikasi; Redis lock hanya bantu, bukan pengganti. Modular monolith tetap.
- Alasan: VPS 2GB memaksa terlalu banyak kompromi performa (queue database 1 worker, cache database, rate-limit lemah). 8 GB memberi headroom realistis untuk Laravel + PostgreSQL + Redis ringan + 2 worker tanpa over-engineering multi-server. Disetujui user 2026-07-13.
- Affects: K1-PRD (§9.5/§9.6/§8.4), K1-PROJECT_OVERVIEW, K1-TECH_VERSION_SEED, K1-ARCHITECTURE, K3-DATABASE_SCHEMA (§ performa), K2-MODULE_AUTH, K2-MODULE_LOOKUP_DATA, K2-MODULE_GUEST_ACCESS, K2-MODULE_PLACEMENT, K4-BACKUP_AND_RECOVERY, K4-DEPLOYMENT, K4-SECURITY_CHECKLIST
- SUPERSEDES: Keputusan “tanpa Redis di MVP / queue driver database + 1 worker karena VPS 2GB” pada entri PROJECT_OVERVIEW 2026-06-28, ARCHITECTURE D5 2026-06-29, MODULE_AUTH A-3/A-4 2026-06-29, MODULE_LOOKUP_DATA D-L1 2026-06-29, dan catatan performa 2vCPU/2GB di DATABASE_SCHEMA/modul terkait. Entri lama ditandai usang untuk constraint infra; domain bisnis tidak diubah.
- Perlu update PRD? Ya — diterapkan ke PRD v0.3.12 (§8.4 queue, §9.5 backup headroom, §9.6 tech stack).
### 2026-07-12 — PRD (v0.3.10 → v0.3.11) + MODULE_GUEST_ACCESS + DATABASE_SCHEMA + DATA_RETENTION_AND_PRIVACY — Pembalikan whitelist Tamu: detail G3 buka Nama + Foto + Riwayat Kerja/Pendidikan penuh
- Keputusan: Atas arahan atasan (via user 2026-07-12), `GuestCandidateView` menjadi **proyeksi berjenjang**. **(G2 daftar) TETAP pseudonim** (identifier = kode kandidat/`nomor_induk`, tanpa nama & foto). **(G3 detail)** kini **membuka** **Nama Alphabet + Nama Katakana**, **Foto** kandidat, **Riwayat Pekerjaan penuh** (`nama_perusahaan` + `perusahaan_penanggung` + bidang + `tanggal_masuk/keluar`), dan **Riwayat Pendidikan penuh** (tingkat + jurusan + `nama_institusi` + `tanggal_masuk/keluar`). **(1) Tidak ada DDL struktural** — seluruh data sudah ada (`candidate.nama_*`, `candidate_photo`, `candidate_work`, `candidate_education`); murni perubahan **proyeksi/kebijakan** + penegakan server-side. `guest_summary` (ringkasan kurasi tamu) **belum ada di skema** → opsional; bila diinginkan, itulah satu-satunya kolom baru (menunggu keputusan terpisah). **(2) Foto Tamu:** signed URL R2 ber-expiry **TTL 15 mnt**, di-generate ulang tiap reload, **di-scope ke ****`guest_link`**** Aktif & belum kadaluarsa**, hanya setelah sesi token valid (custom domain R2/proxy tetap wajib — GAP DEPLOYMENT). **(3) Enforcement:** field yang kini tampil di G3 (nama, foto, nama lembaga, nama perusahaan) **tetap TIDAK boleh jadi parameter sort/filter Tamu** (cegah inferensi/enumerasi); daftar G2 pseudonim. Baris ter-anonimisasi (`pii_anonymized_at`) → nama kosong + foto dihapus → tampil kosong ke Tamu. **(4) Audit:** pembukaan detail tercakup `GUEST_ACCESS` (sukses); enum granular **`GUEST_DETAIL_VIEWED`** kini **DITAMBAHKAN** (keputusan user 2026-07-12, atas rekomendasi agent) — actor NULL, `detail = {token_id, candidate_id, container_id, ip}`; dicatat di PRD Lampiran A (A.1/A.2), DATABASE_SCHEMA §7, MODULE_GUEST_ACCESS §12, DATA_RETENTION_AND_PRIVACY §4.
- Alasan: Perusahaan Jepang perlu identitas & rekam jejak lengkap kandidat pada tahap detail untuk menilai; atasan memutuskan membuka nama/foto/riwayat di detail. PRD = sumber kebenaran; perubahan whitelist = kebenaran inti → versi PRD naik ke v0.3.11. Cakupan dibatasi ke DETAIL (G3); daftar (G2) tetap pseudonim demi minimisasi data. Disetujui user 2026-07-12.
- Affects: K1-PRD (Lampiran C restruktur, §7.7, Riwayat Revisi, versi/judul/footer v0.3.11), K2-MODULE_GUEST_ACCESS (§3.2 G2/§3.2b G3, §3.3 HIDE, §4 sort/filter, §5 TTL foto, §11 test, §12), K3-DATABASE_SCHEMA (§3.3 catatan, komentar `candidate_education.nama_institusi` & `candidate_work.nama_perusahaan`, GAP-DB5 RESOLVED), K4-DATA_RETENTION_AND_PRIVACY (eksposur Nama+Foto eksternal, consent/dasar hukum, TTL, interaksi anonimisasi), K3-UI_WIREFRAME (G2 pseudonim / G3 detail), K4-API_CONTRACTS.
- SUPERSEDES: Menuntaskan **GAP-DB5** (DATABASE_SCHEMA) — nama perusahaan riwayat kerja resmi masuk whitelist (di detail G3). Merestrukturisasi PRD Lampiran C dari whitelist datar (yang sempat memuat nama+foto di level daftar) menjadi berjenjang G2/G3. Tidak menimpa keputusan final lain.
- Perlu update PRD? Ya — SUDAH DITERAPKAN ke v0.3.11 (Lampiran C berjenjang, §7.7, Riwayat Revisi, Versi, judul halaman, footer). Enum audit `GUEST_DETAIL_VIEWED` **DITAMBAHKAN** 2026-07-12 (PRD Lampiran A + DATABASE_SCHEMA §7 + MODULE_GUEST_ACCESS §12 + DATA_RETENTION_AND_PRIVACY). Item governance **DITUTUP** (keputusan user 2026-07-12, atas rekomendasi agent): **(b)** daftar G2 **TETAP pseudonim — tidak dibuka** (minimalisasi data APPI/UU PDP; pola klik-untuk-buka + audit `GUEST_DETAIL_VIEWED` per kandidat; hindari beban penerbitan signed URL foto massal di list). **(c)** kolom `guest_summary` **TIDAK ditambahkan** (YAGNI — belum ada kebutuhan konkret; hindari permukaan PII bebas-teks tambahan; dapat ditambah sebagai kolom nullable non-breaking bila nanti benar diperlukan). Tidak ada perubahan skema/PRD dari penutupan ini (mempertahankan default). DATA_RETENTION_AND_PRIVACY masih draft parsial (owner DPO).
---
### 2026-07-14 — Audit closure Batch A (D-01–D-07) — domain inti siap implementasi
- Keputusan: User menyetujui seluruh rekomendasi Batch A. **(D-01)** Transfer normal Wawancara→Penempatan memakai kandidat `status_wawancara=Siap Dikirim` + availability `Sedang Dipakai`, dengan source participation aktif milik kandidat dan tanpa placement `Bekerja`; approval batch memindahkan ownership secara atomik, availability tetap `Sedang Dipakai`, dan `markInUse()` tidak dipakai untuk flip. Force-Majeur tetap `Tersedia+Disetujui` → `markInUse()`. **(D-02)** `pending_request` menjadi sumber keputusan Checker untuk seluruh approval domain selain `lookup_request` dan `company_request`; untuk domain yang memakainya, status agregat tetap ada, pending+status submission dibuat satu transaksi, dan partial unique membatasi pending aktif per `(type,target_type,target_id)`. `lookup_request.status` dan `company_request.status` menjadi sumber keputusan flow masing-masing; keduanya tidak membuat `pending_request` dan tidak ditambahkan ke `PendingType`. Kedua flow tetap memakai RBAC, `StepUpService`, `AuditLogger`, `NotificationService`, transaksi/rollback, after-commit, self-decision guard, dan anti-double-decision. Payload snapshot tetap wajib untuk batch, Force-Majeur, expel/resign/cancel. **(D-03)** tambah partial unique satu participation Wawancara aktif per kandidat. **(D-04)** kandidat punya status `Draft`; revision memakai snapshot row+child collections, maksimum satu revision Draft/pending aktif per main candidate; approval mengganti seluruh field mutable+child collections secara atomik tanpa mengubah NIK, availability, dan operational history. **(D-05)** anonimisasi diblok bila ada participation/placement/pending/revision aktif dan direvalidasi dalam transaksi. **(D-06)** email menjadi satu-satunya login identifier MVP. **(D-07)** soft-delete/restore Kandidat tidak diekspos di MVP; `deleted_at` dan event terkait hanya reserved/deferred.
- Keputusan F-012: kebenaran bisnis + audit DB commit terlebih dahulu; notifikasi/email Redis dijalankan after-commit; kegagalan enqueue tidak me-rollback transaksi bisnis.
- Alasan: Menutup kontradiksi Critical/High dengan perubahan terkecil yang implementable pada Laravel/PostgreSQL monolith, tanpa status reservasi baru, workflow engine, distributed lock, atau infrastruktur tambahan. Disetujui user 2026-07-14.
- Affects: K1-PRD, K1-STATUS_STATE_MACHINE, K1-BUSINESS_RULES, K2-MODULE_CANDIDATES, K2-MODULE_JOBS, K2-MODULE_PLACEMENT, K2-MODULE_AUTH, K3-DATABASE_SCHEMA, K3-UI_WIREFRAME_NOTES/P4, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, K4-DATA_RETENTION_AND_PRIVACY, K1-ARCHITECTURE (prinsip after-commit; detail lanjutan Batch B).
- SUPERSEDES: Syarat Placement normal `Siap Dikirim + Tersedia`; pola approval campuran tanpa pending untuk container/candidate/batch; kandidat baru langsung `Menunggu Tinjauan-BARU` tanpa Draft; kewenangan soft-delete Kandidat oleh Super Admin; klaim queue Redis sebagai bagian transaksi atomik DB. Keputusan lama hanya usang pada butir tersebut; domain lain tetap berlaku.
- Perlu update PRD? Ya — diterapkan sebagai PRD v0.3.13.
---
### 2026-07-14 — Hygiene pass pasca Fase 3 (label usang)
- Keputusan: Rapikan label dokumentasi non-bisnis agar agent build tidak bingung. **(1)** `STATUS_STATE_MACHINE`: rujukan PRD → v0.3.14; GAP-4 ditandai **RESOLVED** (sudah di PRD sejak v0.3.3); Force-Majeur step-up ditandai **DITETAPKAN tanpa step-up** (bukan \[ASUMSI\]). **(2)** Papan status DECISIONS_LOG: UI_WIREFRAME_NOTES → FINAL v1.0; DATABASE_SCHEMA → FINAL v1.0; API_CONTRACTS/SECURITY/DATA_RETENTION → FINAL v1.2 (Batch B). Tidak mengubah kebenaran domain.
- Alasan: Fase 3 verification menemukan label historis yang menyesatkan; hygiene sebelum agent strategi build.
- Affects: K1-STATUS_STATE_MACHINE, K1-DECISIONS_LOG (papan status saja)
- SUPERSEDES: Label “GAP-PRD terbuka / Force-Majeur \[ASUMSI\] / UI_WIREFRAME Belum mulai” pada papan/ringkasan usang — bukan menimpa entri keputusan Batch A/B.
- Perlu update PRD? Tidak.
### 2026-07-14 — Audit closure Batch B — consistency, security, privacy, dan ops readiness
- Keputusan: **(F-008)** Redis co-located untuk cache/session/queue/rate-limit memakai `maxmemory-policy noeviction`; cache wajib TTL, memory dimonitor, dan Redis bukan source of truth. **(F-012)** kebenaran bisnis+audit+notifikasi in-app DB commit dalam transaksi; email/queue Redis hanya after-commit; gagal enqueue dicatat dan tidak rollback bisnis. **(F-014)** identifier Guest G2/G3 = Nomor Induk `K-YYYY-NNNNN`, tanpa kode `CAND-*` baru. **(F-015)** kandidat `pii_anonymized_at IS NOT NULL` dikeluarkan seluruhnya dari Guest read-model; akses detail langsung ditolak generik. **(F-018)** DESIGN menghapus upload/envelope/signed URL dokumen; Dokumen Peserta = repeatable Jenis+URL Google Drive privat+catatan; hanya foto memakai upload R2. **(F-006)** hierarchy final UI = PRD → fondasi domain/schema/modul/API/security/privacy → UI_WIREFRAME_NOTES → Approved HTML non-authoritative. **(F-013)** DATABASE_SCHEMA difinalkan; `candidate_participation` generik resmi disupersede oleh `participation` + `placement_participants`/service union. **(F-007)** `IDENTITY_DOC_VIEWED` berarti link dokumen diungkap/dibuka melalui aplikasi, bukan bukti file Drive benar-benar dibaca; permission Drive dikelola manual melalui akun/grup staf berwenang dan review offboarding. **(F-010)** audit Auth tidak menyimpan email input mentah: user dikenal→`user_id`; gagal anonim→masked email atau HMAC fingerprint; IP hanya kolom `ip`, tidak digandakan di JSONB. **(F-016)** `FM_REJECTED` ditambahkan sebagai enum audit kanonik. **(F-019)** tanggal akhir kontrak default inklusif = tanggal mulai + durasi bulan − 1 hari; override wajib ≥ tanggal mulai. **(F-017)** minimal satu restore test sukses ke DB temporary menjadi gate go-live. **(F-020)** PROJECT_OVERVIEW dan dokumen aktif dibersihkan dari current-state 2C/2G, no Redis, queue DB, frontend undecided, envelope dokumen, serta `candidate_participation` lama.
- Alasan: Menutup seluruh temuan audit tersisa dengan konfigurasi dan aturan operasional sederhana untuk single VPS/solo developer; tidak menambah platform enterprise atau fitur baru. Disetujui user melalui perintah eksekusi Batch B 2026-07-14.
- Affects: PRD, PROJECT_OVERVIEW, ARCHITECTURE, DATABASE_SCHEMA, MODULE_AUTH, MODULE_GUEST_ACCESS, MODULE_CANDIDATES, MODULE_PLACEMENT, DESIGN, UI_WIREFRAME_NOTES, Approved HTML notes G2/G3/P4, API_CONTRACTS, SECURITY_CHECKLIST, DATA_RETENTION_AND_PRIVACY, DEPLOYMENT, BACKUP_AND_RECOVERY.
- SUPERSEDES: `allkeys-lru` untuk Redis campuran; klaim queue Redis atomik dengan DB; kandidat anonim tampil kosong di Guest; kode tamu `CAND-*`; upload/envelope dokumen; authority NOTES lama di bawah/atas HTML yang ambigu; tabel generik `candidate_participation`; interpretasi audit Drive sebagai bukti file dibaca; email mentah di audit Auth; `FM_REJECTED` non-kanonik; formula akhir kontrak ambigu; backup sukses tanpa restore test sebagai gate; current-state 2C/2G/no Redis/frontend undecided.
- Perlu update PRD? Ya — diterapkan sebagai PRD v0.3.14.
---
### 2026-07-23 — ARCHITECTURE D1 — `internachi/modular` 3.x untuk Laravel 13 stabil
- Keputusan: User menyetujui `internachi/modular` **3.x** untuk mekanisme modularisasi Composer path repositories `app-modules/*` dan Laravel package discovery. Enam batas modul serta aturan public service/facade tetap sama.
- Alasan: `internachi/modular` 3.x mendukung Laravel 13 stabil, sedangkan metadata 2.3.0 hanya mencantumkan Laravel 13 development branch. Perubahan ini menyelaraskan dependency dengan stack Laravel 13 yang terkunci tanpa mengubah arsitektur domain.
- Affects: K1-ARCHITECTURE, Wave 0 W0-T5.
- SUPERSEDES: Versi package `internachi/modular` 2.x pada keputusan D1 tanggal 2026-06-29; pola path repositories, enam modul, dan seluruh keputusan D2–D10 tetap berlaku.
- Perlu update PRD? Tidak — ini sinkronisasi versi dependency, bukan perubahan kebenaran produk atau domain.
### 2026-07-28 — DOC-SYNC-W2-T5-AUTHORITY — Pengecualian sumber keputusan Lookup/Company
- Keputusan: `lookup_request` dan `company_request` adalah pengecualian eksplisit dari `pending_request`. Status pada tabel masing-masing adalah sumber keputusan flow-nya; keduanya tidak membuat baris `pending_request` dan tidak menambah `LOOKUP_REQUEST`/`COMPANY_REQUEST` ke `PendingType`. Keduanya tetap menggunakan fondasi Wave 1: RBAC, `StepUpService`, `AuditLogger`, `NotificationService`, transaksi/rollback, after-commit, self-decision guard, dan anti-double-decision. `pending_request` tetap berlaku untuk approval domain lainnya.
- Alasan: Menutup kontradiksi klaim universal tanpa mengubah schema atau memperkenalkan pola approval baru. Disetujui operator 2026-07-28; repository menjadi sumber keputusan untuk DOC-SYNC ini karena Notion tidak terhubung.
- Affects: K1-PRD §7.4/§7.10, K1-BUILD_INVARIANTS, K1-BUSINESS_RULES, K1-STATUS_STATE_MACHINE, K4-API_CONTRACTS, K4-SECURITY_CHECKLIST, Wave 2 guide, Codex instructions, dan semantic UI notes.
- SUPERSEDES: Hanya frasa universal D-02 bahwa `pending_request` berlaku untuk seluruh approval domain; keputusan D-02 lainnya tetap berlaku.
- Perlu update PRD? Ya — diterapkan sebagai klarifikasi pada PRD v0.3.14 tanpa menaikkan versi.

### 2026-08-01 — DOC-SYNC-REPO-FIRST — Wave 3 closeout dan handoff Wave 4

- Keputusan operasional: Notion tidak pernah terhubung pada repository ini. Selama mode `DOC-SYNC-REPO-FIRST` aktif, committed snapshots, `DECISIONS_LOG.md`, dan root `BUILD_LOG.md` menjadi sumber operasional untuk build; Notion adalah mirror yang dapat disinkronkan kemudian. Konflik antar-snapshot committed tetap menjadi stop condition.
- **BR-CON-03:** `FOR UPDATE` diperbolehkan hanya di dalam transaksi bulk pull Kandidat pada Jobs/W4-T3. Implementasi mengunci baris Kandidat yang akan ditarik, lalu memvalidasi ulang Kandidat `Disetujui` + `Tersedia`, membuat participation, dan memanggil public `CandidateAvailabilityService::markInUse()` sebelum commit. Tidak dipakai pada list/search atau mutasi umum, dan tidak memakai `SKIP LOCKED`; partial unique participation tetap menjadi pengaman akhir.
- **PendingType:** gunakan tipe yang sudah ada untuk Jobs: `IC_CREATE`, `IC_CLOSE`, `IC_EXPEL`, dan `GUEST_LINK`. `lookup_request`/`company_request` tetap tidak membuat `pending_request` dan tidak menambah `PendingType`. Resubmit main Kandidat tetap memakai `CANDIDATE_NEW` karena main row belum pernah disetujui; audit `CANDIDATE_REVISION_SUBMITTED` adalah event historis dan tidak memerlukan tipe pending baru.
- Alasan: Menghilangkan ketergantungan operasional pada koneksi Notion tanpa mengubah arsitektur, schema, atau kontrak approval; keputusan langsung memenuhi lingkup W4 bulk pull.
- Affects: repository `AGENTS.md`, `BUILD_LOG.md`, playbook Build Log, dan handoff W4-T3. Tidak mengubah PRD atau daftar tipe database.
- SUPERSEDES: Ketergantungan bahwa Build Log Notion harus tersedia untuk memulai wave; tidak menimpa keputusan domain Batch A/B.
- Perlu update PRD? Tidak.

### 2026-08-05 — DOC-SYNC-W4-GAP3-FROZEN-AT — Slot aktif partisipasi vs freeze close

- Keputusan: Menutup celah skema antara GAP-3 (freeze tanpa status `status_wawancara` baru + re-entry baris baru + `Tersedia`) dan partial unique yang sebelumnya hanya memfilter empat status non-terminal. **(1)** Kolom denormalized `participation.frozen_at TIMESTAMPTZ NULL` diresmikan di DATABASE_SCHEMA. **(2)** “Participation mengisi slot aktif” = `status_wawancara` ∈ \{Menunggu Wawancara, Lulus, Proses Dokumen, Siap Dikirim\} **dan** `frozen_at IS NULL`. **(3)** Partial unique `uq_participation_one_active` memasukkan predikat `AND frozen_at IS NULL`. **(4)** Saat `IC_CLOSE` disetujui: dalam transaksi yang sama, stamp `frozen_at` pada baris non-terminal kontainer itu (**tanpa** mengubah `status_wawancara`), lalu `markAvailable()` kandidat terkait → `Tersedia`. **(5)** Guard service pull/eligibility memakai definisi slot yang sama; baris beku tidak memblok tarik ke kontainer `Aktif` lain. **(6)** BR-AVL-01, BR-CON-05, BR-PII-08, BUILD_INVARIANTS, STATUS_STATE_MACHINE GAP-3, MODULE_JOBS, dan playbook Wave 4 diselaraskan. Bukan status mesin baru; bukan keputusan Placement/Guest.
- Alasan: Unique index PostgreSQL tidak dapat join ke `interview_container`; denormalisasi `frozen_at` adalah representasi fisik minimal agar freeze GAP-3, BR-AVL-01, dan re-entry dapat ditegakkan bersama. Operator meminta DOC-SYNC dulu sebelum perbaikan kode W4-R1. Mode repo-first: repository adalah sumber operasional (Notion belum terhubung).
- Affects: K3-DATABASE_SCHEMA, K1-STATUS_STATE_MACHINE, K1-BUSINESS_RULES, K1-BUILD_INVARIANTS, K2-MODULE_JOBS, playbook Wave 4, rencana implementasi W4-R1.
- SUPERSEDES: Hanya interpretasi partial unique participation yang mengabaikan freeze (unique hanya atas empat status tanpa `frozen_at`). GAP-3 niat bisnis (blok transisi, status terakhir tetap, availability Tersedia, baris baru untuk re-entry) **tetap berlaku** dan kini punya penegakan skema.
- Perlu update PRD? Tidak — PRD Sub-flow tutup kontainer sudah mensyaratkan freeze partisipasi + ketersediaan Tersedia; ini penajaman skema/implementasi.
